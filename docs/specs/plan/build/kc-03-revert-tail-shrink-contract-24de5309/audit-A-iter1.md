<!-- Auto-generated by chunk-auditor agent. Do not edit. -->

# Audit A, iteration 1 — kc-03-revert-tail-shrink-contract

**Cycle hex:** 24de5309
**Auditor model:** opus
**Auditor agent version:** 1
**Date:** 2026-06-09
**Plan reference:** [./plan.md](./plan.md)
**Final verdict:** PASS

## Summary

KC-03 converges the pinned revert arm onto the abandon arm's already-correct tail-shrink (Rule 1/3), appends the breadcrumb after original content with a single label (Rule 2a), and makes the abandoned-list truthful — all 7 Audit-A claims PASS. The fix is render-pass-only: `streaming.rs` (the `enforce_call_atomic_backstop` belt) is absent from the diff; `build_trunk_context` and the node model are untouched; the forensic `self.messages` re-fetch (former clause iii) is REMOVED, so the helper no longer reads the forensic log and is now an associated fn (no `&self`). No public-API / persisted-field / migration / tool-arg-schema change; the vestigial `tag_on_call_node` param was removed with zero external callers (it was a private fn). Full lib suite 251 passed / 0 failed (244 baseline + 7 NEW), `collapse_abandon` 36 passed (band [36,41]); clippy `-Dwarnings` + `fmt --check` clean.

| Claim | Verdict | Evidence-anchor |
|---|---|---|
| 1. `retain_pinned_calls_on_node` clause (iii) removed; off-trunk → drop by id; on-trunk → keep; associated fn | PASS | context.rs:789 (`fn …(trunk: &mut [AgentMessage], idx)`), :838-839 keep, :841-852 drop; diff removes `results_to_append`/`self.messages` re-fetch |
| 2. `weave_braking_annotations` marker leading, annotation appended, `reverted past:` stripped | PASS | context.rs:937/966 marker, :38/:968 append, :956-959 strip |
| 3. `apply_revert` truthful-list: `pinned_onto_result` skips step (i); strictly-after seeds; active-ptr/reject unchanged | PASS | run.rs:845 `pinned_onto_result`, :875-879 skip, :882-887 span; reject/active-ptr not in diff |
| 4. NEW contract cells green (Rules 1/2/2a/3 + truthful-list + single-label + no-400) | PASS | context.rs:4086/4120/4162/4201/4241/4264; run.rs:1666; test run 36+1 green |
| 5. No-400 invariant holds; `enforce_call_atomic_backstop` UNCHANGED | PASS | streaming.rs:130 (not in diff); `matrix_no_dangling_or_orphan_after_pinned_shrink` green |
| 6. Kernel-minimality: blast radius = 3 render fns; no public API/persisted/migration/schema change; zero leakage | PASS | diff-stat src/ = context.rs+run.rs only; grep returns streaming.rs:237 prod + run.rs test + doc-comments |
| 7. clippy `-Dwarnings` + `fmt --check` | PASS | both ran clean in audit shell (PASS-with-caveat — gate-4 authoritative) |

## Per-claim detail

### Claim 1 — F1 Rule 1+3: `retain_pinned_calls_on_node` clause (iii) re-append REMOVED; off-trunk drop by id; on-trunk keep; converges to abandon arm; now associated fn
**Verdict:** PASS
**Evidence:**
- `src/types/context.rs:789` — signature is now `fn retain_pinned_calls_on_node(trunk: &mut [AgentMessage], idx: usize)` — no `&self`, no `tag_on_call_node` param. The diff (`git diff dev`) shows the old `fn retain_pinned_calls_on_node(&self, trunk: &mut Vec<AgentMessage>, idx: usize, tag_on_call_node: bool)` replaced. Param type also narrowed `&mut Vec` → `&mut [AgentMessage]` (slice; consistent with no `.insert`).
- Clause (i) on-trunk keep — `context.rs:830-840`: per call, `on_trunk = trunk.iter().any(… ToolResult { tool_call_id } if tool_call_id == call_id)`; `if on_trunk { continue; }` (line 838-839) keeps the call. Matches "on-trunk → keep".
- Clause (ii)/Rule 3 off-trunk drop by id — `context.rs:841-852`: the off-trunk branch pushes `ids_to_drop.push(call_id.clone())` (line 842), then `content.retain(|b| !matches!(b, Content::ToolCall { id } if ids_to_drop.contains(id)))` (lines 848-850). Surgical removal by id from the kept node. The off-trunk result is never re-appended.
- Clause (iii) REMOVED — the diff deletes the entire `results_to_append` vector, the `self.messages.iter().find(…)` forensic re-fetch (former `:842-852`), and the trailing `for (offset, result) in results_to_append … trunk.insert(idx + 1 + offset, result)` re-append loop (former `:866-868`). No `trunk.insert` of an off-trunk sibling remains in the body.
- Associated-fn / no forensic-log read — the only `self.messages` access in the old body (the re-fetch) is gone; the doc-comment at `context.rs:784-788` states "the forensic `self.messages` is NOT read or mutated here … so this is now an associated fn, not a `&self` method". The call-site at `context.rs:757` is `Self::retain_pinned_calls_on_node(&mut trunk, cn_idx)` (was `self.retain_pinned_calls_on_node(&mut trunk, cn_idx, node_is_call)`).
- Converges to abandon arm: the abandon arm (`context.rs:557-690`, unchanged) shrinks + drops; pinned now does the same drop, differing only by Rule 2 (handled in weave / apply_revert). Confirmed via `row_pinned_revert_to_first_result_now_call_atomic` (now asserts `!trunk_has_tool_result("pcall_b")` + `!trunk_has_dangling_call`) — test green.

### Claim 2 — F1 Rule 2a: `weave_braking_annotations` marker leading + annotation appended + double-label collapsed
**Verdict:** PASS
**Evidence:**
- Marker leading — `context.rs:937` `let marker = format!("[{}]", node_id.render());` then `prepend_marker_to_message(&mut lm.message, &marker)` at `context.rs:966`. `prepend_marker_to_message` (`context.rs:12`) prefixes the leading text block. Marker stays LEADING.
- Annotation appended after content — a NEW fn `append_annotation_to_message` (`context.rs:38`) suffixes the leading text block: `*text = format!("{text} {annotation}")` (`context.rs:46`). Called at `context.rs:967-969`: `if !annotation.is_empty() { append_annotation_to_message(&mut lm.message, &annotation); }`. Render order is `[n<id>] <body> [<kind>: …]`. Verified by `pinned_revert_summary_appended_after_original_content` (`context.rs:4162`) asserting `body_at < annot_at` + `starts_with("[n3]")`.
- `reverted past:` prefix stripped → single label — `context.rs:956-959`: `let text = tag.text.strip_prefix("reverted past: ").unwrap_or(&tag.text);` then `annotation.push_str(&format!("[{}: {}]", kind, text))`. The old code (`marker.push_str(&format!(" [{}: {}]", kind, tag.text))`) used the raw `tag.text` (which carried `reverted past:`). Verified by `revert_breadcrumb_single_label_not_double` (`context.rs:4241`) asserting `[outcome: parallel cluster]` present AND `reverted past:` ABSENT.

### Claim 3 — F1 truthful-list: `apply_revert` excludes kept target's own calls; active-pointer + reject UNCHANGED
**Verdict:** PASS
**Evidence:**
- `pinned_onto_result` gate — `src/agent_loop/run.rs:841-845`: `target_is_result_node = matches!(&context.messages[target_idx], AgentMessage::Llm(lm) if matches!(lm.message, Message::ToolResult { .. }))` and `pinned_onto_result = !request.category.tag_kind().is_decayable() && target_is_result_node`. Pinned (Outcome/Checkpoint) are non-decayable; abandon (Lesson/Finding) are decayable → keep step (i).
- Step (i) skipped when pinned-onto-result — `run.rs:873-879`: `if !pinned_onto_result { if let AgentMessage::Llm(lm) = &context.messages[target_idx] { push_call_names(lm, &mut names); } }`. The seed of the target cluster's own calls is conditional. Abandon-class keeps step (i).
- Strictly-after span always seeds — `run.rs:882-887` (the `for m in &context.messages[target_idx + 1..]` loop) is unconditional.
- Active-pointer + reject UNCHANGED — `git diff dev -- src/agent_loop/run.rs` shows the only production hunk is this name-set carve-out (lines 819-887). The active-pointer assignment (`context.active_node_id = Some(request.target)`) and the User-strictly-after reject rule are not in the diff.
- Verified by `pinned_revert_onto_result_breadcrumb_names_only_shrunk_tools` (`run.rs:1666`): a pinned completion revert onto first-result n2 names `skill_help` (shrunk) and NOT `memory_help` (kept). Test green.

### Claim 4 — NEW contract cells (Tier A, §8) green
**Verdict:** PASS
**Evidence:** All cells exist and pass (`cargo test --lib collapse_abandon` → 36 passed / 0 failed; the run.rs cell → 1 passed):
- Rule 1 `matrix_pinned_revert_shrinks_tail_after_target` (`context.rs:4086`) — asserts `pcall_a` kept, `pcall_b`/`pcall_c` gone across Outcome×Checkpoint × arity 2/3.
- Rule 3 `matrix_pinned_revert_removes_orphan_call_by_id` (`context.rs:4120`) — `!trunk_has_dangling_call` + n1 keeps `pcall_a`, drops `pcall_b` by id.
- Rule 2a `pinned_revert_summary_appended_after_original_content` (`context.rs:4162`) — `starts_with("[n3]")`, `body_at < annot_at`, single-label `[<label>: parallel cluster]` present, double-label `[<label>: reverted past:` absent.
- Rule 2 `abandon_revert_summary_replaces_target_content` (`context.rs:4201`) — abandon REPLACES heavy body (gone); pinned ADDS while keeping `Wrote 356 bytes`.
- single-label `revert_breadcrumb_single_label_not_double` (`context.rs:4241`).
- no-400 `matrix_no_dangling_or_orphan_after_pinned_shrink` (`context.rs:4264`).
- truthful-list `pinned_revert_onto_result_breadcrumb_names_only_shrunk_tools` (`run.rs:1666`) — names skill_help shrunk, NOT memory_help kept.

### Claim 5 — No-400 invariant holds; `enforce_call_atomic_backstop` UNCHANGED
**Verdict:** PASS
**Evidence:**
- `enforce_call_atomic_backstop` at `src/agent_loop/streaming.rs:130`; `git diff dev --name-only -- src/agent_loop/streaming.rs` returns empty (file NOT in diff) → backstop unchanged.
- No-400 across exercised cells: `matrix_no_dangling_or_orphan_after_pinned_shrink` (`context.rs:4264`) calls `assert_no_400` over Outcome×Checkpoint × arity 2/3 × first/mid-result; `matrix_pinned_into_cluster_shrinks_tail` and `matrix_pinned_revert_shrinks_tail_after_target` also call `assert_no_400`. All green in the 36-test run. `assert_no_400` checks `!trunk_has_dangling_call` + `!trunk_has_orphan_result`.

### Claim 6 — Kernel-minimality
**Verdict:** PASS
**Evidence:**
- Blast radius / production files: `git diff dev --stat` shows code changes only in `src/types/context.rs` (3 render concerns: `retain_pinned_calls_on_node` + `weave_braking_annotations` + new `append_annotation_to_message` helper) and `src/agent_loop/run.rs` (`apply_revert` name-set). `streaming.rs` untouched. `build_trunk_context` / node model not in diff.
- Forensic `self.messages` never mutated: the only former mutation-adjacent read (clause iii re-fetch) is removed; remaining accesses in `apply_revert` are `&context.messages[…]` reads. No `self.messages` write introduced.
- No public-API / persisted-field / migration / tool-arg-schema change: `git diff dev -- src/types/context.rs | grep '^[+-].*pub (fn|struct|enum)'` returns empty (the `pub fn weave_braking_annotations` signature `Vec<AgentMessage> -> Vec<AgentMessage>` is unchanged — not in the +/- hunks). The removed `tag_on_call_node` param belonged to a PRIVATE fn (`fn`, no `pub`); `grep -rn 'retain_pinned_calls_on_node\|tag_on_call_node'` over src/ + tests/ minus context.rs returns ZERO → no external caller breaks.
- Consumer-leakage grep `collapse_abandon_class_cluster|retain_pinned_calls_on_node` in src/ minus context.rs returns: `streaming.rs:237` (production call-site) + `run.rs:1872` (test harness call) + `streaming.rs:113` and `run.rs:1761` (doc-comment cross-refs). The plan §3.A predicted "streaming.rs:237 prod + run.rs test"; the additional two hits are doc-comments (not invocations). Sub-agent/parallel/evaluation paths do NOT appear → zero blast radius outside the brake render. NOTE: plan task-prompt cited `run.rs:1757` for the test; actual is `run.rs:1872` (call) / `:1761` (doc) — a benign line-number drift in the plan citation, not a code gap; the substance (only streaming prod + run.rs test/doc) holds.

### Claim 7 — clippy `-Dwarnings` + `fmt --check`
**Verdict:** PASS (PASS-with-caveat — orchestrator gate-4 MUST-RUN authoritative)
**Evidence:** Both ran successfully in the audit shell:
- `RUSTFLAGS="-Dwarnings" cargo clippy -j 4 --manifest-path … --all-targets` → `Finished dev profile … target(s)` with no warnings/errors.
- `cargo fmt --manifest-path … -- --check` → no output (clean).
- Caveat: observed PASS in audit shell; orchestrator gate-4 MUST-RUN remains authoritative. `cargo clean` run after the test invocations per discipline (reclaimed 1.6 GiB).

## Workspace test result
- Command: `/root/rust-env/cargo/bin/cargo test -j 4 --manifest-path /root/projects/phi/phi-core/Cargo.toml --lib` (single crate, no `--workspace` per phi-core kernel lane)
- Output: 251 passed / 0 failed / 0 ignored (lib). `collapse_abandon` subset: 36 passed / 0 failed. Truthful-list cell: 1 passed.
- Plan §8 expected delta: +6 to +10 NEW (244 → 250-254 total lib; collapse_abandon 30 → 36-41). Actual: 251 total lib (+7), 36 collapse_abandon (+6).
- Match: ✅ (251 within [250,254]; 36 within [36,41])

## CI guards
- phi-core has NO `scripts/check-*.sh` CI guard set at v0 (plan §0 + skill project-context). N/A — paperwork-side note only.
- check-doc-links.sh: N/A (phi-core no guards)
- check-ops-doc-headers.sh: N/A
- check-phi-core-reuse.sh: N/A (kernel does not consume itself — phi-core-leverage-check is N/A)
- check-spec-drift.sh: N/A
- k8s-readiness-check: N/A (library, not daemon)

## Final verdict
**PASS.** All 7 Audit-A claims verified with file:line evidence. The pinned revert arm converges to the abandon arm's tail-shrink (clause iii re-append removed; off-trunk results dropped + orphan calls removed by id; on-trunk keep preserved), the breadcrumb is appended after original content with a single label, and the abandoned-list truthfully names only shrunk tools. Render-pass-only: `streaming.rs`/backstop, `build_trunk_context`, and the node model are untouched; forensic log read-only (re-fetch removed); no public-API/persisted/migration/schema change; the vestigial `tag_on_call_node` param removed with zero external callers. Lib suite 251/0, collapse_abandon 36/0 (within plan §8 bands), clippy `-Dwarnings` + `fmt --check` clean (PASS-with-caveat — gate-4 authoritative). One benign observation (not a FAIL): the plan task-prompt cited `run.rs:1757` for the test harness call; the actual call is `run.rs:1872` with a doc-comment at `:1761` — a line-number drift in the plan citation only; the substantive consumer-leakage claim (only streaming prod + run.rs test/doc) holds.
