<!-- Auto-generated by chunk-auditor agent. Do not edit. -->

# Audit C, iteration 1 — kc-03-revert-tail-shrink-contract

**Cycle hex:** 24de5309
**Auditor model:** opus
**Auditor agent version:** 1
**Date:** 2026-06-09
**Plan reference:** [./plan.md](./plan.md)
**Final verdict:** PASS

## Summary

Audit C verifies the **carry-forward regression posture** for KC-03 on **project=phi-core** (kernel lane). KC-03 INVERTS the KC-02 keep-whole tests to the simple tail-shrink contract (R1/R2/R2a/R3) and relies on a STAY-green set (on-trunk-keep gate + abandon-class convergence template + weave echo invariant + no-400 backstop).

**Scope-correction note (load-bearing):** the archived plan §6/§7/§11 say "3 inversions"; the orchestrator AUTHORIZED a correction to **5** at P0 (the plan undercounted — it lumped the 3 edge inversions under the "matrix" re-author and mis-analyzed `matrix_pinned_last_result_and_post_cluster_no_loss` as a SPLIT). The actual diff inverts exactly **5** KC-02 keep-whole tests, and `matrix_pinned_last_result_and_post_cluster_no_loss` STAYS verbatim (reverts onto the tip → nothing after X). The audit confirms all 5 inversions are INTENDED (encode the removed clause-(iii) keep-whole misread), correctly inverted with specific SHRINK assertions, and are the ONLY keep-whole test inversions. No collateral inversion or weakened assertion found.

| Claim | Verdict | Evidence-anchor |
|---|---|---|
| 1. 5 KC-02 keep-whole inversions INTENDED + correctly inverted + ONLY ones | PASS | context.rs:3293/3746/3922/3963/3983; diff hunks invert `trunk_has_tool_result`→`!trunk_has_tool_result` |
| 2. STAY-green bodies UNCHANGED (6 tests incl. last_result onto-tip) | PASS | 0 diff mentions of all 6 names; context.rs:3887/3789/3068/3323/3703/2508 |
| 3. Abandon-class arm UNCHANGED (convergence template) | PASS | 0 diff mentions of `matrix_abandon_class`; collapse `if`-arm (`:557-690`) untouched |
| 4. Weave `[n<id>]`-echo invariant GREEN (6 tests) | PASS | 0 diff mentions of `weave_braking_annotations_`; context.rs:1623 asserts `starts_with`/`contains` |
| 5. 2 in-place updates = superseded-literal class (not collateral) | PASS | run.rs:1456 (`[lesson:` single-label) + revert.rs:244 (R1/R2 literals); sibling stored tag.text:1452 stays green |
| 6. No-400 across cells; full suite green | PASS | lib 251 + 28 binaries 0-fail (executed); streaming.rs UNCHANGED; collapse_abandon 36 |
| 7. Live disposition-asserting close-gate posture STATED | PASS | plan §10:267-273 names R1/R3/R2a/R2 + single-label + no-400, both drivers × completion+failure |

**Overall:** 7/7 PASS. Regression posture intact; the 5 inversions are intended (not collateral); STAY-green set + abandon arm + weave echo + backstop all preserved; full crate suite green at the gate-2 numbers (lib 244→251, collapse_abandon 30→36, 28 binaries / 0 fail). One non-blocking observation: a stale module-level COMMENT block (`context.rs:3555-3564`) still narrates the KC-02 (B) keep-whole disposition — a doc-fidelity nit (Audit-B surface), NOT a test assertion (does not violate claim 1's "no OTHER keep-whole test asserting a re-append").

## Per-claim detail

### Claim 1 — The 5 KC-02 keep-whole inversions are INTENDED, correctly inverted, and the ONLY keep-whole inversions
**Verdict:** PASS
**Evidence:**

All 5 named tests are present in the post-edit tree and their diffs INVERT the keep-whole assertion to a SHRINK assertion. `git -C phi-core diff dev -- src/types/context.rs` fn-signature changes: `matrix_pinned_into_cluster_keeps_whole_cluster`→`matrix_pinned_into_cluster_shrinks_tail`, `edge_post_cluster_followon_dropped`→`edge_post_cluster_everything_after_x_dropped`, `edge_mixed_on_off_trunk`→`edge_mixed_on_off_trunk_shrinks_tail`; `row_pinned_revert_to_first_result_now_call_atomic` + `edge_two_cluster_isolation` body-changed without rename. That is exactly 5 modified existing collapse_abandon tests; the only other added fns are the 6 NEW contract cells + the `rendered_node_text` helper.

- **`row_pinned_revert_to_first_result_now_call_atomic`** (`context.rs:3293`): assertion flipped `trunk_has_tool_result(&collapsed,"pcall_b")` → `!trunk_has_tool_result(&collapsed,"pcall_b")` ("the off-trunk call_b sibling is shrunk (tail-shrink contract, Rule 1)"); `!trunk_has_dangling_call` (orphan removed, `:3301-3304`); `trunk_has_tool_result("pcall_a")` kept on-trunk (`:3306-3309`). Diff comment: "KC-03 inversion (SUPERSEDES the KC-02 F1(B) keep-whole) … the orphan paired call is surgically removed by id (Rule 3)."
- **`matrix_pinned_into_cluster_shrinks_tail`** (`context.rs:3746`, renamed): a2 first-result `pcall_a && !pcall_b`; a3 first-result `pcall_a && !pcall_b && !pcall_c`; a3 mid-result `pcall_a && pcall_b && !pcall_c` — all shrink-after-X, each with `assert_no_400`. Renamed from `…_keeps_whole_cluster`.
- **`edge_post_cluster_everything_after_x_dropped`** (`context.rs:3922`, renamed): `pc_0 (on-trunk) && !pc_1 (shrunk)`; the `!trunk_has_tool_result("seq_c")` (sealed-past follow-on dropped) assertion STAYS — its justification text changed from "membership distinguishes it" to "strictly after X". Orphan-call-removed semantics covered by the dropped sibling.
- **`edge_mixed_on_off_trunk_shrinks_tail`** (`context.rs:3963`, renamed): revert onto mid result n3; `pcall_a && pcall_b && !pcall_c` — `pcall_c` (past tip, strictly after X) shrunk; a,b kept.
- **`edge_two_cluster_isolation`** (`context.rs:3983`): `c1a && c1b` (cluster1 kept verbatim, cross-cluster isolation); `c2a && !c2b` — `c2b` (after X) shrunk + orphan call removed; isolation preserved.

Each asserts the SHRINK disposition with specific `!trunk_has_tool_result(...)` / `!trunk_has_dangling_call` assertions (not weakened to a bare `assert_no_400`). All 5 carry `KC-03 inversion … SUPERSEDES the KC-02 F1(B) keep-whole … ADR-0003 §D3.1` comments → encode the removed clause-(iii) keep-whole misread.

**ONLY-inversions check:** `grep 're-append\|whole cluster\|membership'` over the test module returns no remaining test-BODY assertion of a re-append; the call-node-reclaim test (`pinned_class_keeps_cluster_whole_no_dangling_call:2508`) asserts "the result is NOT re-appended" (clause ii reclaim, consistent with the contract). Module-level comment `context.rs:3555-3564` still describes the KC-02 (B) keep-whole verbatim — a doc-fidelity stale-narrative nit (NOT a test assertion); flagged as observation, does not falsify the claim.

### Claim 2 — STAY-green (bodies UNCHANGED)
**Verdict:** PASS
**Evidence:**
`git diff dev -- src/types/context.rs | grep -c` of the 6 STAY-green test names returns **0** (no name appears in any changed line or hunk-header function context → bodies unmodified). Tests confirmed present:
- `matrix_pinned_last_result_and_post_cluster_no_loss` (`context.rs:3887`): reverts onto the post-cluster seq result n5 (all results on-trunk, nothing after X); asserts `pcall_a && pcall_b` kept verbatim + `assert_no_400`. The plan's "SPLIT" guidance was a mis-analysis — the test reverts onto the tip, so nothing shrinks; correctly kept verbatim. Not modified.
- `matrix_on_trunk_pinned_cluster_kept_always` (`:3789`), `pinned_cluster_mid_trunk_kept_verbatim` (`:3068`), `row_pinned_revert_to_call_node_now_call_atomic` (`:3323`), `matrix_pinned_onto_call_node_reclaims_to_breadcrumb` (`:3703`), `pinned_class_keeps_cluster_whole_no_dangling_call` (`:2508`, M=1 reclaim asserting "result NOT re-appended" + "no dangling call survives") — all absent from the diff.
All 6 pass in the targeted run (see Workspace test result).

### Claim 3 — Abandon-class arm UNCHANGED (convergence template)
**Verdict:** PASS
**Evidence:**
`git diff dev -- src/types/context.rs | grep -c 'matrix_abandon_class'` = **0**; both `matrix_abandon_class_call_node_first_result_symmetric` (`:3823`) and `matrix_abandon_class_asymmetry_is_benign` (`:3857`) are untouched and pass. The production diff is confined to the pinned-cluster `else` branch (hunk `@@ -689,30 +715,28`) + the `retain_pinned_calls_on_node` helper; the abandon-class `if`-arm (`collapse_abandon_class_cluster:557-690`) is above line 689 and not in the diff → no abandon-path code change. The pinned arm CONVERGES to it (only Rule 2 ADD-vs-REPLACE differs), per the rewritten helper doc-comment (`context.rs:770-788`).

### Claim 4 — Weave `[n<id>]`-echo invariant GREEN
**Verdict:** PASS
**Evidence:**
`git diff dev -- src/types/context.rs | grep -c 'fn weave_braking_annotations_'` = **0** → all 6 echo tests (`:1612/:1635/:1644/:1659/:1709/:1723`) have unchanged bodies. `context.rs:1623` asserts `text.starts_with("[n7]")` (marker leading) + `text.contains("[lesson: …]")` + `text.contains("explored the npm path")` — substring/prefix checks with no ordering constraint between annotation and content, so the append-after-original split (which only relocates the `[kind: text]` annotation after content while the `[n<id>]` marker stays leading) keeps them green. `weave_braking_annotations_marks_untagged_nodes` (`:1640`) uses `assert_eq!(text,"[n3] state your name")` but for an UNTAGGED node (no annotation), unaffected by the append change. All 6 pass in the targeted run.

### Claim 5 — Two additional in-place test updates are the SAME superseded-literal class (not collateral)
**Verdict:** PASS
**Evidence:**
1. `apply_revert_tests::abandoned_branch_content_not_reintroduced` (`run.rs:1456`): assertion updated `all_text.contains("reverted past: wrote plan-v1.md")` → `all_text.contains("[lesson: wrote plan-v1.md")` — the single-label render (the tag's `reverted past:` prefix is stripped at weave time to avoid the `[lesson: reverted past: …]` double-label, ADR-0003 §D3.2). A direct consequence of the authorized single-label deliverable. The SIBLING test asserting the STORED tag text — `assert_eq!(tag.text, "reverted past: approach v1 was tangential")` (`run.rs:1452`) — is NOT in the diff and stays green, confirming the prefix-strip is RENDER-only (the stored breadcrumb keeps the prefix).
2. `RevertTool` `description_teaches_four_categories_as_budget_levers` (`revert.rs:244`): OLD assertions checked superseded literals `"do NOT shrink the kept span"` + `"completion/step-summary reclaim the abandoned tail; if there's no tail …"`; NEW assertions check `"every revert SHRINKS the tail after X"` + `"ADDED to X after its original content, which is KEPT"` + `"REPLACES X's content"` + the empty-tail no-op. A direct consequence of the authorized tool-doc rewrite deliverable (§5; the description string at `revert.rs:105` was rewritten to R1/R2/R2a/R3). Both are superseded-literal updates following the load-bearing deliverables, not collateral bugs.

### Claim 6 — No-400 across exercised cells; full suite green
**Verdict:** PASS (executed in audit)
**Evidence:**
`streaming.rs` has an EMPTY diff stat vs dev → `enforce_call_atomic_backstop` (`streaming.rs:130`) UNCHANGED. The no-400 invariant test `matrix_no_dangling_or_orphan_after_pinned_shrink` (`context.rs:4264`) asserts `assert_no_400` across arity-2/3 first/mid-result pinned-shrink cells; `matrix_no_dangling_or_orphan_across_all_cells` (`:4027`) + the per-test `assert_no_400` calls cover the exercised cells. Executed:
- `cargo test --lib collapse_abandon` → **36 passed; 0 failed** (matches gate-2 30→36).
- `cargo test --lib` → **251 passed; 0 failed** (matches gate-2 244→251).
- `cargo test` (full crate, lib + 28 integration binaries) → all `test result: ok`, **0 failed** across every binary (matches gate-2 "28 binaries ok / 0 fail").
- `cargo clean` run immediately after (reclaimed 4.9 GiB) per discipline.

### Claim 7 — Live DISPOSITION-asserting close-gate posture STATED
**Verdict:** PASS
**Evidence:**
Plan §10 "Live DISPOSITION-asserting close-gate" (`plan.md:267-273`) names ALL six criteria and explicitly asserts the DISPOSITION (not merely no-400): (1) post-target sibling result ABSENT (Rule 1 tail shrunk); (2) orphaned call REMOVED from the call node (Rule 3); (3) pinned breadcrumb APPENDED at the kept node, "original body FIRST" (Rule 2a); (4) breadcrumb names ONLY the shrunk tool with a SINGLE label (render nits); (5) `failure`/abandon category REPLACES X's content (Rule 2); (6) no provider 400 in any cell. Across BOTH drivers (`deepseek-chat-v3-0324` + strongest-available open-source) × `completion` (pinned) and `failure` (abandon) categories. The header states this is "NOT merely no-400 — that is the wrong-invariant trap KC-02 fell into" — the explicit KC-02 wrong-invariant lesson. Criteria are STATED and assert the disposition. (Orchestrator-run live round-trip; auditor confirms the criteria text only, per the claim.)

## Workspace test result
- Command: `/root/rust-env/cargo/bin/cargo test -j 4 --manifest-path /root/projects/phi/phi-core/Cargo.toml` (+ `--lib` and `--lib collapse_abandon` targeted)
- Output: lib **251 passed / 0 failed / 0 ignored**; `collapse_abandon` **36 passed / 0 failed**; 28 integration binaries all ok / **0 failed**
- Plan §8 expected delta: collapse_abandon 30 → 36-40 (band [36,41]); total lib 244 → 250-254
- Match: ✅ (collapse_abandon 36 within band; lib 251 within band; matches gate-2 numbers exactly)
- `cargo clean` run post-test (4.9 GiB reclaimed) per CH-18 discipline.

## CI guards
phi-core has NO `check-*.sh` CI guard set at v0 (only `scripts/pre-commit` + `scripts/install-hooks.sh`) — confirmed via `ls phi-core/scripts/`. The MUST-RUN list IS the gate (clippy `-Dwarnings` + test + `fmt --check`), closed by the orchestrator at gate-4.
- check-doc-links.sh: N/A (phi-core has no guard set)
- check-ops-doc-headers.sh: N/A (phi-core has no guard set)
- check-phi-core-reuse.sh: N/A (kernel does not consume itself — phi-core-leverage-check is N/A)
- check-spec-drift.sh: N/A (phi-core has no guard set)
- k8s-readiness-check: N/A (library, not a daemon)
- clippy `-Dwarnings` + `fmt --check`: NOT-EXECUTED-IN-AUDIT (orchestrator MUST-RUN at gate-4 per plan §1; gate-2 reported clippy+fmt clean)

## Final verdict
**PASS.** All 7 Audit-C claims PASS. The carry-forward regression posture is intact and the inversions are INTENDED, not collateral: exactly 5 KC-02 keep-whole tests inverted to specific SHRINK assertions (each encoding the removed clause-(iii) keep-whole misread, with `!trunk_has_tool_result`/`!trunk_has_dangling_call` + `assert_no_400`), and they are the only keep-whole test inversions. The STAY-green set (6 tests incl. `matrix_pinned_last_result_and_post_cluster_no_loss` kept verbatim — the plan's "split" was a mis-analysis), the abandon-class convergence template, the 6 weave echo tests, and the unchanged `enforce_call_atomic_backstop` backstop are all preserved (zero diff mentions). The 2 in-place run.rs/revert.rs updates are the same superseded-literal class (single-label render + tool-doc R1/R2 rewrite), with the stored-tag-text sibling staying green confirming the prefix-strip is render-only. The full crate suite is green at the gate-2 cardinalities (lib 251, collapse_abandon 36, 28 binaries / 0 fail). The §10 live close-gate posture STATES all six disposition criteria across both drivers × both categories, asserting the disposition (the KC-02 wrong-invariant lesson). One non-blocking observation handed to Audit-B/orchestrator: stale module-level comment `context.rs:3555-3564` still narrates the KC-02 (B) keep-whole disposition (doc-fidelity nit, not a test assertion). No failing claims; no weakened assertions; no collateral inversion.
