<!-- Auto-generated by chunk-auditor agent. Do not edit. -->

# Audit A, iteration 1 — kc-04-selectable-skill-prompt-layout

**Cycle hex:** 91a175a4
**Auditor model:** opus
**Auditor agent version:** 1
**Date:** 2026-06-12
**Plan reference:** [./plan.md](./plan.md)
**Final verdict:** PASS

## Summary

| Claim | Verdict | Evidence-anchor |
|---|---|---|
| 1. `SkillPromptFormat` enum + derives + `#[default] Xml` + re-exports | PASS | `skills.rs:61-68`; `lib.rs:71`; `context/mod.rs:37` |
| 2. `format_for_prompt_as` exists; Xml arm verbatim; `format_for_prompt` delegates; empty→`""` | PASS | `skills.rs:275-319`, `:260-262`, `:276-278` |
| 3. Golden test pins exact XML bytes (not substring) | PASS | `skills.rs:787-812` (`assert_eq!`) |
| 4. F2.b correctness: exhaustive trigger set; C-2 type-preservation; C-1 colon/bare; round-trip | PASS | `skills.rs:507-626`, `:851-926` |
| 5. `with_skills_format` on BasicAgent; `with_skills` delegates Xml; wiring test both branches | PASS | `basic_agent.rs:507-532`, `:1948-1976` |
| 6. Kernel-minimality: additive-only, zero consumer leakage, helpers private | PASS | commit `65af84d` stat; greps empty; `skills.rs:507/522/583` `fn` |
| 7. Test count 589 (band [589,592]); clippy/fmt | PASS-with-caveat | 6 new tests confirmed; clippy/test/fmt orchestrator-closed |
| 8. Forbidden-duplication: one YAML emitter; no parallel renderer | PASS | grep `available_skills:` = `skills.rs:300` only |

## Per-claim detail

### Claim 1 — `SkillPromptFormat { Xml, Yaml }` + derives + re-exports
**Verdict:** PASS
**Evidence:** `skills.rs:61-68`: `#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)] pub enum SkillPromptFormat { #[default] Xml, Yaml }` — all 6 derives present, `#[default]` on `Xml`. Re-exported at `lib.rs:71` `pub use context::skills::{SkillPromptFormat, SkillSet};` and `context/mod.rs:37` `pub use skills::{SkillPromptFormat, SkillSet};` — both alongside `SkillSet`.

### Claim 2 — `format_for_prompt_as` / verbatim Xml arm / delegation / empty
**Verdict:** PASS
**Evidence:** `skills.rs:275` `pub fn format_for_prompt_as(&self, format: SkillPromptFormat) -> String`. Empty guard `:276-278` returns `String::new()` BEFORE the match — applies to both formats. Xml arm `:281-298` is byte-for-byte the historical body (commit diff confirms only a one-level indent shift into the match arm; string literals `<available_skills>\n`, `  <skill>\n`, `</available_skills>` and `xml_escape` calls identical). `format_for_prompt()` `:260-262` is `self.format_for_prompt_as(SkillPromptFormat::Xml)`.

### Claim 3 — Golden byte-for-byte pin
**Verdict:** PASS
**Evidence:** `skills.rs:787-812` `kc04_golden_xml_byte_for_byte` builds the exact `expected` string (`:797-802`) and `assert_eq!(skills.format_for_prompt(), expected)` (`:805`) + `assert_eq!(...format_for_prompt_as(Xml), expected)` (`:806-809`) + `assert!(!expected.ends_with('\n'))` (`:811`). Exact-bytes equality, not `.contains()`.

### Claim 4 — F2.b correctness (load-bearing). Sub-claim granularity (v12):
**Verdict:** PASS `(a)trigger-set ✓ + (b)C-2 type-preserve ✓ + (c)C-1 colon/bare ✓ + (d)round-trip ✓`
**Evidence:**
(a) `yaml_escape_scalar` (`:507`, private `fn`) → `yaml_scalar_needs_quoting` (`:522`). Trigger coverage: empty `:524`; leading/trailing whitespace `:529`; structural chars `:` `#` `\n` `\t` `\r` `"` `'` `\` `:534-535`; leading indicators `- ? , [ ] { } & * ! | > % @ \` ~` `:542-561`; reserved words any-case via `to_ascii_lowercase` `true/false/null/yes/no/on/off/~` `:565-571`; numeric/float/hex/octal/date via `looks_numeric_or_date` `:583-625`. Escape order backslash-first `:510-514`. Exhaustive per plan §1 F2.
(b) C-2 `kc04_yaml_reserved_words_and_numbers_preserve_string_type` `:885-926` asserts `null`/`true`/`42`/`-7`/`3.14`/`0x1F`/`0o17`/`2026-06-12` etc. each QUOTE (`:907-910`) AND round-trip via `serde_yaml::from_str` recovering as `as_str() == Some(raw)` (`:913-919`) — i.e. STRING not bool/null/int/date.
(c) C-1 `kc04_yaml_decision_boundary_colon_quotes_plain_stays_bare` `:851-879`: `:`-value quotes (`:862`), plain `weather` stays bare (`:859-860`), both recover (`:865-871`), helper-level boundary asserts (`:874-878`).
(d) Tier B `kc04_yaml_round_trips_through_serde_yaml` `:816-846` parses via `serde_yaml::from_str` and recovers name/description/location (`:834-842`).

### Claim 5 — `with_skills_format` wiring
**Verdict:** PASS
**Evidence:** `basic_agent.rs:518-532` `pub fn with_skills_format(mut self, skills, format) -> Self` renders `skills.format_for_prompt_as(format)` with the same empty-skip + `\n\n`-join. `with_skills` `:507-509` delegates `self.with_skills_format(skills, SkillPromptFormat::Xml)`. Wiring test `kc04_with_skills_format_reaches_system_prompt` `:1948-1976`: YAML reaches `system_prompt` (`:1966-1967`) + no `<available_skills>` (`:1968`); default `with_skills` still emits XML (`:1973-1974`) + no `available_skills:` key (`:1975`).

### Claim 6 — Kernel-minimality
**Verdict:** PASS
**Evidence:** Commit `65af84d` source files = only `skills.rs`, `basic_agent.rs`, `lib.rs` (1 line), `context/mod.rs` (1 line) — all additive. `grep prompt_format|PromptFormat` minus `SkillPromptFormat` → 0 hits (no config struct, no TOML field, no string-parse). No `Skill`/`SkillSet` struct change, no serde/migration/persisted-field. `grep pub fn yaml_escape_scalar|yaml_scalar_needs_quoting|looks_numeric_or_date` → 0 (all private `fn` at `:507/:522/:583`).

### Claim 7 — Test count / clippy / fmt
**Verdict:** PASS-with-caveat (observed PASS in audit shell for static count; orchestrator MUST-RUN gate remains authoritative)
**Evidence:** Exactly 6 new `kc04_*` tests confirmed via grep (5 in `skills.rs:788/817/852/886/930`, 1 in `basic_agent.rs:1949`). 583 baseline + 6 = 589, inside band [589,592]. clippy `-Dwarnings`, full `cargo test`, and `fmt --check` are orchestrator-closed MUST-RUN items — **NOT-EXECUTED-IN-AUDIT** in this sandbox; orchestrator confirmed all three green per prompt.

### Claim 8 — Forbidden-duplication
**Verdict:** PASS
**Evidence:** `grep "available_skills:"` → emitter only at `skills.rs:300` (other hits are basic_agent.rs test assertions + a doc-comment `:66`). `grep -E "fn .*format.*skill|fn .*skill.*prompt" | grep -v format_for_prompt` → only the test fn `kc04_with_skills_format_reaches_system_prompt` (not a renderer). One YAML emitter; no parallel skill-render path.

## Workspace test result
- Command: `/root/rust-env/cargo/bin/cargo test -j 4 --manifest-path /root/projects/phi/phi-core/Cargo.toml` — NOT-EXECUTED-IN-AUDIT (orchestrator MUST-RUN; sandbox defers)
- Static evidence: 6 new `kc04_*` tests added
- Plan §8 expected delta: +6 (583→589, band [589,592])
- Match: ✅ (static count); runtime count orchestrator-closed

## CI guards
- phi-core has no `scripts/check-*.sh` guard set at v0 (paperwork-side note per ci-guards-run v2 phi-core branch). The MUST-RUN list (clippy/test/fmt) IS the gate; orchestrator-closed.
- phi-core-leverage-check: N/A (kernel does not consume itself); substituted by kernel-minimality check (Claim 6 PASS).
- k8s-readiness-check: N/A (phi-core is a library).

## Final verdict
PASS. All 8 claims verified with file:line evidence. The chunk is purely-additive: XML default is byte-for-byte preserved (golden-pinned at `skills.rs:805`), the F2.b conditional bare-vs-quoted escaping carries an exhaustive trigger set with the highest-value reserved-word/numeric type-preservation MUST-SHIP test (C-2 `:885`) asserting STRING round-trip via serde_yaml, the `with_skills_format` wiring reaches the system prompt for YAML while keeping XML the default, and kernel-minimality holds (zero consumer leakage; all escape helpers private). Claim 7's clippy/test/fmt are NOT-EXECUTED-IN-AUDIT (orchestrator MUST-RUN); the static +6 test count matches plan §8. No FAIL claims.
