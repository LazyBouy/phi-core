<!-- Auto-generated by chunk-auditor agent. Do not edit. -->

# Audit B, iteration 1 — kc-05-progressive-tool-disclosure

**Cycle hex:** a0830ed3
**Auditor model:** opus
**Auditor agent version:** 1
**Date:** 2026-08-19
**Plan reference:** [./plan.md](./plan.md)
**Final verdict:** PASS

## Summary

| Claim | Verdict | Evidence-anchor |
|---|---|---|
| 1. ADR-0005 Accepted + 7 sections + §D5.1–7 disposition notes (KEY: §D5.4 honest F1.a/F2.b; §D5.7 provider rationale) | PASS | `docs/decisions/0005-progressive-tool-disclosure.md:5,9,25,33,63,70,80,89` + §D5.4 L47-49 vs code (validate not auto-invoked) + §D5.7 L59-61 |
| 2. 6 docs updated + verified-headers 2026-08-19 + [EXISTS] tags; no stale narrative | PASS | 6× `head -1` = 2026-08-19; new surfaces tagged [EXISTS]; stale-phrase grep = 0 hits |
| 3. Cycle-index row a0830ed3: Phases=6, Auditors=3(A+B+C), Iterations=pending, Status=in-flight | PASS | `_cycle-index.md:23` |
| 4. Doc–code alignment; no [PLANNED]/[CONCEPTUAL] mislabel; default_tools()=7 unchanged | PASS | all 9 surfaces grep-confirmed in `src/`; PLANNED/CONCEPTUAL grep=0; `concepts/tools.md:177-178` |

Audit B (docs/ADR/verified-headers/cycle-index) is clean. ADR-0005 is `Accepted` with all 7 required top-level sections and seven sub-decisions each carrying a labeled disposition note. The §D5.4 KEY CHECK holds: the ADR documents the F1.a hard-error as the validator's DEFAULT stance exposed as a public primitive the consumer wires — "phi-core exposes the primitive but does **not** force-invoke it on the hot path" (L48-49) — and code confirms `validate_tool_registration` appears only in its definition + tests, never in a build/hot path, so the F2.b consumer-override is genuinely preserved (NOT overstated as an automatic build-time gate). All 6 §3.C docs carry 2026-08-19 verified-headers with [EXISTS]-tagged new surfaces; no stale pre-KC-05 narrative survives. Cycle-index row matches. phi-core-leverage-check + k8s-readiness-check are N/A (kernel library); no `check-*.sh` CI guards exist for phi-core — orchestrator MUST-RUN gate-4 (clippy/test/fmt) closes the executable envelope.

## Per-claim detail

### Claim 1 — ADR-0005 Accepted; 7 sections; §D5.1–§D5.7 labeled disposition notes; §D5.4 honest; §D5.7 provider rationale
**Verdict:** PASS
**Evidence:**
- `docs/decisions/0005-progressive-tool-disclosure.md:5` — `**Status: Accepted**`.
- All 7 top-level sections present per plan §5: `## Forks` (L9), `## Context` (L25), `## Sub-decisions` (L33), `## Cross-references` (L63), `## Consequences` (L70), `## Revisit triggers` (L80), `## Verification` (L89).
- §D5.1–§D5.7 each carry a labeled disposition note: §D5.1 "Net-new surface — no prior short/detailed behaviour to preserve" (L37); §D5.2 "Pre-existing-behaviour: … preserved byte-for-byte on the OFF path" (L41); §D5.3 "Pre-existing-behaviour: the static hand-written `with_default_help` map path is retained" (L45); §D5.4 "Net-new primitive" (L49); §D5.5 "Additive" + "Pre-existing-behaviour preservation note (net-new field)" (L53); §D5.6 "**Behaviour changed (model-facing, intentional — this IS the fix)**" (L57 — the revert split); §D5.7 "Net-new reduced-wire shape — no prior reduced render to preserve" (L61).
- **KEY CHECK §D5.4 (honesty of F1.a+F2.b):** L48-49 states "Per F2.b, phi-core exposes the primitive but does **not** force-invoke it on the hot path — the enforcement *stance* is a consumer-overridable default, so no policy is baked into the kernel." Cross-checked against code: `grep -rn "validate_tool_registration" src/` returns only the definition (`types/tool.rs:398`), doc-comments, and test-site references (`types/tool.rs:562/573/590/599/612`, `tools/revert.rs:339/358` — both `#[cfg(test)]`). There is NO invocation in `basic_agent.rs`/`build_config`/`streaming.rs` or any build/hot path. The ADR does NOT overstate it as an automatic build-time gate → HONEST. The one non-test call site described in the ADR ("a kernel test asserts every `default_tools()` built-in passes validation", L49) is itself a test, consistent with the not-auto-invoked claim.
- **§D5.7 provider-acceptance rationale:** L59-61 gives it — "Anthropic REQUIRES `input_schema` to be an object, so F4.a (omit) would be rejected AND would force a breaking `Option<..>` wire-type change"; the stub, not omit, is chosen; "Both provider test fixtures already ship `{"type":"object"}`, so the stub is proven-accepted."
- Bonus: ADR Cross-references P0 path `…/kc-05-progressive-tool-disclosure-a0830ed3/p0-investigation.md` (L68) resolves to an existing file (`ls` confirmed). (Note: plan §1 L9 cites an older `_p0-investigations/109-110-…-p0.md` path that does NOT exist — a plan-narrative staleness, but the ADR cross-ref itself is valid; outside Audit-B claim scope, flagged as observation.)

### Claim 2 — 6 docs updated + verified-headers 2026-08-19 + [EXISTS] tags + no stale narrative
**Verdict:** PASS
**Evidence:**
- `head -1` on all 6 docs → each carries `<!-- Last verified: 2026-08-19 by Claude Code (KC-05: …) -->`: `concepts/tools.md:1`, `architecture/algorithms/core/streaming.md:1`, `specs/developer/tool.md:1`, `reference/configuration.md:1`, `reference/api.md:1`, `reference/glossary.md:1`.
- New surfaces documented with [EXISTS] tags: `concepts/tools.md:220` "Status: [EXISTS] (KC-05)" for the Progressive section, trait split at L43-52 + validation primitive L59-66; `streaming.md:33` "KC-05 [EXISTS]" reduced branch + L37 "Default OFF ⇒ … BYTE-IDENTICAL to the historical full form"; `developer/tool.md:11-12,45-47,55-57` [EXISTS] default methods + registration table; `configuration.md:48-73` `progressive_tool_catalog` field + `ProgressiveToolCatalog` struct (KC-05) [EXISTS] default OFF; `api.md:123` `with_progressive_tool_catalog` builder; `glossary.md:18` progressive tool-catalog term.
- No stale pre-KC-05 narrative: `grep -rni "always full|serves only a static|only a static map|catalog is always|no short/detailed|…"` across all 6 docs returned 0 hits.

### Claim 3 — Cycle-index row a0830ed3
**Verdict:** PASS
**Evidence:** `_cycle-index.md:23` — `| a0830ed3 | [kc-05-progressive-tool-disclosure](…) … | 6 | 3 (A+B+C) | pending | \`in-flight\` | pending |`. Phases=6 ✓, Auditors=3 (A+B+C) ✓, Iterations=pending ✓, Status=`in-flight` ✓ (orchestrator owns the flip; not retro-complete, as expected).

### Claim 4 — Doc–code alignment; no [PLANNED]/[CONCEPTUAL] mislabel; default_tools()=7
**Verdict:** PASS
**Evidence:**
- Every doc-claimed [EXISTS] surface is confirmed in `src/`: `fn short_description` (`types/tool.rs:298`), `fn detailed_description` (`types/tool.rs:308`), `fn is_mcp_adapter` (`types/tool.rs:320`; override `mcp/tool_adapter.rs:136`), `SHORT_DESCRIPTION_MAX_CHARS` (`types/tool.rs:333`), `validate_tool_registration` (`types/tool.rs:398`), `tool_registration_warning` (`types/tool.rs:426`), `struct ProgressiveToolCatalog` (`agent_loop/config.rs:410`), `fn engages` (`config.rs:438`), `fn with_progressive_tool_catalog` (`agents/basic_agent.rs:517`).
- No mislabel: `grep -rni "PLANNED|CONCEPTUAL"` filtered to KC-05 surface terms across the 4 primary docs returned 0 hits — no shipped surface tagged [PLANNED]/[CONCEPTUAL].
- `default_tools()` count unchanged: `concepts/tools.md:177-178` — "`default_tools()` returns the 6 core coding tools (…) plus `tool_help` (7 total)."

## Workspace test result
- Command: NOT RUN by this auditor (brief mandates verify-by-reading; do NOT run cargo). Plan §8 accept band [609, 615], predicted ≈611 (589 baseline + 20 NEW). Marked `NOT-EXECUTED-IN-AUDIT` — orchestrator MUST-RUN gate-4 (`cargo test -j 4 --manifest-path …`) is authoritative.
- Cross-check: ADR Verification block (L95) asserts "609 passed (589 baseline + 20 NEW)"; consistent with plan §8. Not independently executed here.

## CI guards
- phi-core has **no `scripts/check-*.sh` guard set** at v0 (confirmed by plan §3 L187 "No `scripts/check-*.sh` exist" + phi-core CLAUDE.md). ci-guards-run → paperwork-side note; N/A. Gate-4 MUST-RUN list (clippy `-Dwarnings` + `cargo test` + `fmt --check`) IS the gate — orchestrator-owned.
- phi-core-leverage-check: **N/A** (kernel does not consume itself; plan §3 substitutes the kernel-minimality surface check — outside Audit-B docs lane).
- k8s-readiness-check: **N/A** (phi-core is a library; plan §3.B all 7 axes N/A).

## Final verdict
**PASS.** All 4 Audit-B claims verified against `file:line` evidence. ADR-0005 is `Accepted`, structurally complete (7 sections + §D5.1–§D5.7 with per-sub-decision disposition notes), and the §D5.4 KEY CHECK holds — the hard-error is documented honestly as the validator's DEFAULT stance exposed as a consumer-wired primitive (never auto-invoked in the kernel build path, confirmed by code grep), preserving the F2.b override; §D5.7 carries the Anthropic `input_schema`-object provider rationale for stub-not-omit. All 6 §3.C docs carry 2026-08-19 verified-headers with [EXISTS]-tagged new surfaces and zero stale pre-KC-05 narrative; every doc-claimed surface exists in code; cycle-index row matches (6 phases / 3 auditors / pending / in-flight). No FAIL or PARTIAL claims. cargo/test-count + CI-guard-equivalents are `NOT-EXECUTED-IN-AUDIT` (verify-by-reading brief + no phi-core guard scripts) and defer to the orchestrator gate-4 MUST-RUN list.
