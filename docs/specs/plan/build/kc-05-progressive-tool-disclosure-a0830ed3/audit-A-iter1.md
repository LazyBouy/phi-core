<!-- Auto-generated by chunk-auditor agent. Do not edit. -->

# Audit A, iteration 1 — kc-05-progressive-tool-disclosure

**Cycle hex:** a0830ed3
**Auditor model:** opus
**Auditor agent version:** 1
**Date:** 2026-08-19
**Plan reference:** [./plan.md](./plan.md)
**Final verdict:** PASS

## Summary

Audit A (code + tests + kernel-minimality) of the CLOSED phi-core kernel chunk KC-05. All 8 claims verified by reading source (`file:line` cited); cargo test/clippy/fmt NOT run in-audit per brief (orchestrator ran the authoritative MUST-RUN GREEN at gate-2: 609 passed / 0 failed / 5 ignored; clippy `-Dwarnings` clean; fmt clean). No source modified. phi-core has no `check-*.sh` CI guards (N/A). All 8 claims PASS. One documented refinement on claim 6 (the tool_help-reference carry-forward test legitimately stays on the SHORT `description()` — plan §6 explicitly permitted "and/or the short one-liner").

| Claim | Verdict | Evidence-anchor |
|---|---|---|
| 1. Trait split — 3 default methods only | PASS | `types/tool.rs:298-300, 308-310, 320-322` (all default bodies) |
| 2. Registration validation (F1.a+F2.b) | PASS | `types/tool.rs:333, 398, 342-383`; validator NOT auto-invoked (grep) |
| 3. Reduced render; OFF arm byte-identical | PASS | `streaming.rs:291-310`; git-diff OFF arm == pre-KC-05 |
| 4. Config predicate (F5-rec) | PASS | `config.rs:410-421, 423-431, 438-444` |
| 5. tool_help catalog source; cycle-free | PASS | `tools/tool_help.rs:29-37, 47, 56, 134, 145, 108` |
| 6. revert split + 3 re-targeted tests | PASS | `tools/revert.rs:107-113 (242c), 115-121, 237/267/313` |
| 7. Kernel-minimality forbidden-leakage | PASS | grep = 1 pre-existing hit `config.rs:361` only |
| 8. 20 NEW tests / final 609 | PASS | 3+3+3+3+4+2+2=20 structural; run NOT-EXECUTED-IN-AUDIT |

## Per-claim detail

### Claim 1 — Trait split (F-token #110): 3 default methods, defaults only
**Verdict:** PASS
**Evidence:** `types/tool.rs:298-300` `fn short_description(&self)->&str { self.description() }`; `:308-310` `fn detailed_description(&self)->Option<&str> { None }`; `:320-322` `fn is_mcp_adapter(&self)->bool { false }`. All three carry default bodies (the `timeout()` precedent at `:285`); no new non-default trait method was added, so all existing impls (12 kernel + 3 i-phi = "15 existing impls" per test comment `tool.rs:443`) compile unchanged. Compile-of-all-impls itself is NOT-EXECUTED-IN-AUDIT (orchestrator gate-2 GREEN backs it).

### Claim 2 — Registration validation (F1.a + F2.b); KEY DESIGN CHECK
**Verdict:** PASS
**Evidence:** `SHORT_DESCRIPTION_MAX_CHARS: usize = 256` (`tool.rs:333`); `validate_tool_registration(&dyn AgentTool)->Result<(),ToolRegistrationError>` (`:398-417`); enum `ToolRegistrationError { MissingName, MissingShortDescription{tool}, ShortDescriptionTooLong{tool,len,max} }` (`:342-361`) with `Display` (`:363-383`) + `Error` (`:385`). KEY DESIGN CHECK — the validator is a PUBLIC PRIMITIVE returning `Result`, NOT auto-invoked on the kernel hot path: `grep validate_tool_registration src/` returns only test-module usages (`revert.rs:339, 358` both inside `#[cfg(test)] mod tests` opened at `revert.rs:228`; plus `tool.rs` inline tests). No production agent-build call site (agent.rs/sub_agent.rs/basic_agent.rs/agent_loop). Rustdoc `tool.rs:394-397` states it plainly: "phi-core exposes this primitive but does not force-invoke it on the hot path... a consumer calls this to opt into the hard-error stance, or ignores it." This is the coherent joint realization of F1.a (hard-error is the primitive's default stance — over-limit/empty returns `Err`) + F2.b (consumer wires it + owns handling; auto-invoking would break the override). It does NOT fail F1.a's "fails fast at boot" intent — the fail-fast is delivered when the consumer calls the primitive; the kernel additionally ships `kc05_default_tools_pass_registration_validation` (`revert.rs:351`) as a CI-guard test asserting every built-in stays compliant.

### Claim 3 — Reduced render (F3.b/F4.b); OFF arm byte-identical
**Verdict:** PASS
**Evidence:** `streaming.rs:291` `let progressive = config.progressive_tool_catalog.engages(&context.tools);`. ON arm `:296-301`: `description = t.short_description()`, `parameters = json!({ "type": "object" })`. OFF `else` arm `:302-308`: `description = t.description()`, `parameters = t.parameters_schema()`. `git diff HEAD -- streaming.rs` shows the pre-KC-05 removed block (`- name/description()/parameters_schema()`) is field-for-field identical to the new `else` arm — the only change is the `if progressive {…} else {…}` wrapper. Golden pin: `progressive_tool_catalog_test.rs:221 golden_off_wire_is_byte_identical`. OFF arm confirmed unchanged.

### Claim 4 — Config predicate (F5-rec)
**Verdict:** PASS
**Evidence:** `config.rs:410-421` `struct ProgressiveToolCatalog { enabled: bool, min_tools: usize, engage_on_mcp: bool }`; `:423-431` `Default = { false, 8, true }`; `:438-444` `engages(&[Arc<dyn AgentTool>])->bool` = `if !self.enabled { return false }` then `tools.len() > self.min_tools || (self.engage_on_mcp && tools.iter().any(|t| t.is_mcp_adapter()))` — equivalent to `enabled && (len>min_tools || (engage_on_mcp && any mcp))`. Field added additively after `provider_wire_sink` (`:394`).

### Claim 5 — tool_help catalog source (F3.b); cycle-freedom
**Verdict:** PASS
**Evidence:** `tool_help.rs:29-37` `ToolCatalogEntry { detailed: Option<String>, schema: serde_json::Value }` — holds owned DATA, NO `Arc<dyn AgentTool>` back-reference → cycle-free (rustdoc `:42-46, 88-93` confirm); `:47` `type ToolCatalogSnapshot = BTreeMap<String, ToolCatalogEntry>`; `:56` `build_tool_catalog_snapshot(&[Arc<dyn AgentTool>])`; `:134 with_catalog` + `:145 catalog_slot()` install seam (BasicAgent fills at build_config). Static-map `with_default_help` (`:108`) fallback preserved; `execute` prefers catalog then falls back (`:199-233`); `catalog_miss_falls_back_to_static_map` test (`:510`) proves fallback.

### Claim 6 — revert split (F1.a deliverable 6)
**Verdict:** PASS (with documented refinement)
**Evidence:** `revert.rs:107-113` `description()` is a lean 242-char one-liner (measured; ≤ 256) ending `…Call tool_help("revert_to_state") for the full tree model and worked examples.`; `:115-121` `detailed_description()` returns `Some(REVERT_DETAILED)` (`:226`, measured 1872 chars ≈ 1870). The 3 carry-forward tests are PRESENT and re-targeted, NOT deleted: `description_teaches_tree_node_model_and_forward_continuation` (`:237`) asserts `.detailed_description()` (`:246`); `description_teaches_four_categories_as_budget_levers` (`:267`) asserts `.detailed_description()` (`:278`); `description_names_the_tool_help_doc_reference` (`:313`) asserts `.description()` (`:319`). REFINEMENT: the brief simplified all three to "re-targeted to detailed_description()", but the tool_help-reference test correctly stays on the SHORT `description()` — the tool_help pointer lives on the one-liner (`:112`), and plan §6 (`:310-311`) explicitly permitted "move to `detailed_description()` and/or the short one-liner". Load-bearing requirement (predicate-anchored, none deleted) met.

### Claim 7 — Kernel-minimality forbidden-leakage grep
**Verdict:** PASS
**Evidence:** `grep -rniE "i-phi|iphi|per.agent.policy|daemon|assemble" tool_help.rs types/tool.rs config.rs` returns exactly 1 hit: `config.rs:361` "when downstream operators (e.g. i-phi's braking config) need a" — the `revert_render_policy` rustdoc, a PRE-EXISTING KC-01/02/03 surface, NOT KC-05. KC-05's new surface (tool.rs methods/const/validator, tool_help.rs snapshot, config.rs `progressive_tool_catalog`) carries zero consumer-policy references. Matches the claim exactly.

### Claim 8 — Tests: 20 NEW MUST-SHIP; final 609
**Verdict:** PASS (structural); run NOT-EXECUTED-IN-AUDIT
**Evidence:** Structural count of NEW tests per Tier: A=3 (`tool.rs:517/528/539`), E=4 (`tool.rs:556/567/582/604`), D=3 (`tool_help.rs:433/455/510`), B=3 (`progressive_tool_catalog_test.rs:221/237/265`), C=3 (`…:291/303/315`), F=2 (`revert.rs:326/351`; plus 3 count-neutral re-targeted), G=2 (`anthropic.rs:899`, `openai_compat.rs:900`). Total = 20, exactly plan §8. 589 baseline + 20 = 609 = accept-band floor [609,615]. My own cargo run = NOT-EXECUTED-IN-AUDIT (orchestrator gate-2 already GREEN: 609 passed / 0 failed / 5 ignored). Cascade note: `grep 'AgentLoopConfig {'` = 34 sites (plan predicted 32 = 1 struct-def + 31 literals); +2 = new KC-05 test literals in `progressive_tool_catalog_test.rs`, well under the >47 pause threshold — additive, consistent.

## Workspace test result
- Command: `cargo test -j 4 --manifest-path .../Cargo.toml` — **NOT-EXECUTED-IN-AUDIT** (brief directive; orchestrator ran it at gate-2).
- Output (orchestrator gate-2, authoritative): 609 passed / 0 failed / 5 ignored.
- Plan §8 expected delta: +20 (baseline 589 → 609; band [609,615]).
- Match: ✅ (609 = floor; 20 NEW structurally confirmed).

## CI guards
- phi-core has **no** `scripts/check-*.sh` guard set at v0 → **N/A** (paperwork-side note; MUST-RUN list is the gate, closed by orchestrator at gate-4).
- check-doc-links.sh: N/A (not present for phi-core)
- check-ops-doc-headers.sh: N/A
- check-phi-core-reuse.sh: N/A (kernel does not consume itself; phi-core-leverage-check N/A)
- check-spec-drift.sh: N/A

## Final verdict
**PASS.** All 8 claims verified against source with `file:line` evidence. The trait split ships as default methods only (backcompat by construction); the registration validator is correctly a public `Result`-returning primitive NOT force-invoked on the kernel hot path (the coherent F1.a+F2.b joint realization); the reduced-render OFF arm is byte-identical to the pre-KC-05 code (git-diff confirmed) and golden-pinned; the config predicate matches the F5-rec spec exactly; the tool_help catalog source is cycle-free (owns data, no `Arc` back-ref) with static-map fallback preserved; the `revert` split lands a 242-char one-liner + 1872-char detailed body with all 3 carry-forward tests re-targeted (none deleted); forbidden-leakage grep returns only the 1 pre-existing KC-01/02/03 hit; and all 20 NEW tests are structurally present with the final count 609 confirmed by the orchestrator's gate-2 GREEN. Execution claims (clippy `-Dwarnings` / fmt / cargo test) are NOT-EXECUTED-IN-AUDIT per brief and close at orchestrator gate-4. No failing claims.
