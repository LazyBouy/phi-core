<!-- Auto-generated by chunk-auditor agent. Do not edit. -->

# Audit A, iteration 1 — kc-06-mock-provider-repeat-mode

**Cycle hex:** 256ff295
**Auditor model:** opus
**Auditor agent version:** 1
**Date:** 2026-08-26
**Plan reference:** [./plan.md](./plan.md)
**Final verdict:** PASS

## Summary

Audit A covers KC-06 code + tests + kernel-minimality + back-compat on the phi-core KERNEL lane (read-only; grounded on the uncommitted `dev` working-tree diff vs baseline `929d326`). All 7 claims PASS. KC-06 is purely additive: one private `repeat: bool` field, two additive constructors (`with_repeat`/`repeating`), and a ~4-line rotate-on-pop cycle inside the existing `Mutex` lock scope in `stream`. The `repeat == false` path is behaviourally identical to today's `responses.remove(0)`, and the empty→`"(no more mock responses)"` fallback is untouched. Back-compat is verified by construction: the baseline 133 `MockProvider::{new,text,texts}` call-sites go to 137 purely additively (`^-` diff grep = 0 removed/edited; +4 = 3 in `mock.rs` [2 inline test call-sites + 1 doc cross-ref] + 1 in the new `mock_repeat_test.rs`). The forbidden-leakage grep on the kernel surface returns 0 (no i-phi / TOML / consumer-policy concept). Semver correctly bumped 0.12.0 → 0.13.0 (additive MINOR; no public signature removed/changed). Only `src/provider/mock.rs` changed in `src/`; `agent_loop/`, `provider/traits.rs`, `types/` untouched. The single caveat is claim 7 (a documentation-density overrun, expressly permitted by plan §3.B).

| Claim | Verdict | Evidence-anchor |
|---|---|---|
| 1. Rotate-on-pop; `repeat==false` behaviourally identical; fallback untouched; thread-safe; `MockResponse: Clone` | PASS | `mock.rs:209-216` (rotate) / `:205-207` (fallback untouched) / `:204,218` (lock scope) / `:72` (Clone derive) |
| 2. Ctors `with_repeat`/`repeating` + private field; `new`/`text`/`texts` signatures + behaviour unchanged | PASS | `mock.rs:121` / `:133-135` / `:96` / `:104` / `:138` / `:143` |
| 3. Back-compat / ZERO external cascade; 133 sites byte-identical; no test flips | PASS | `^-.*MockProvider::(new\|text\|texts)` = 0; baseline 133 → current 137 (+4 additive) |
| 4. Kernel-minimality: forbidden-leakage grep on `mock.rs` = 0 | PASS | grep EXIT=1 (no match) |
| 5. Semver 0.13.0 additive MINOR; no public signature removed/changed | PASS | `Cargo.toml:3` `version = "0.13.0"` |
| 6. Only `mock.rs` + `Cargo.toml` + new test file changed in code | PASS | `git diff --stat 929d326 -- src` = only `src/provider/mock.rs`; agent_loop/traits/types empty diff |
| 7. §3.B LOC: logic within budget; +63 overrun is rustdoc | PASS (with note) | logic ~12-15 LOC; overrun = §D6.4 rustdoc + ctor doc-comments (plan §3.B escape) |

## Per-claim detail

### Claim 1 — Rotate-on-pop (`mock.rs:209-216`): non-empty `else` branch is `remove(0)` + conditional `push(front.clone())`; `repeat==false` byte-identical; empty→fallback (`:205-207`) untouched; thread-safe; `MockResponse: Clone`
**Verdict:** PASS
**Evidence:** `src/provider/mock.rs:208-217` reads verbatim:
```
} else {
    let front = responses.remove(0); // pop the front response
    if self.repeat {
        // Repeat/cycle mode: re-queue the popped response to the back so the
        // flat queue cycles verbatim (never exhausts while non-empty). The
        // `repeat == false` path above is byte-identical to `responses.remove(0)`.
        responses.push(front.clone());
    }
    front
}
```
This matches the plan §1 F3.a binding exactly. For `repeat == false` the `if` guard is skipped, so the branch does `remove(0)` and returns `front` with no rotation — behaviourally identical to the baseline `responses.remove(0)` (diff hunk shows the baseline line `responses.remove(0) // pop the front response` was refactored into `let front = responses.remove(0); ... front`; the observable output for `repeat == false` is unchanged). The empty→fallback branch (`mock.rs:205-207`, `if responses.is_empty() { … MockResponse::Text("(no more mock responses)".into()) }`) is context-only in the diff hunk `@@ -150,7 +206,14 @@` — untouched. Thread-safe: the rotate executes inside the single `std::sync::Mutex` scope opened at `mock.rs:204` (`let mut responses = self.responses.lock().unwrap();`) and dropped at `:218` (`// MutexGuard dropped here — lock released`). `MockResponse` derives `Clone` at `mock.rs:72` (`#[derive(Debug, Clone)]` above `pub enum MockResponse`), satisfying `front.clone()`.
**Note (stale anchor, fact holds):** the audit prompt cites `mock.rs:52` for the `Clone` derive; post-diff it is at `:72` (shifted down by the added module rustdoc). The derive exists regardless — the anchor drift is cosmetic, not a defect.

### Claim 2 — Ctors: `with_repeat` (`:121`) mirrors `new`; `repeating` (`:133`) = `with_repeat(_, true)`; private `repeat: bool` (`:96`); `new`/`text`/`texts` set `repeat:false` internally, signatures + behaviour unchanged
**Verdict:** PASS
**Evidence:** `mock.rs:121` `pub fn with_repeat(responses: Vec<MockResponse>, repeat: bool) -> Self { Self { responses: std::sync::Mutex::new(responses), repeat } }` — mirrors `new` (`:101-106`) and threads the `repeat` bool. `mock.rs:133-135` `pub fn repeating(responses: Vec<MockResponse>) -> Self { Self::with_repeat(responses, true) }` — exactly `with_repeat(_, true)`. Private field `repeat: bool` at `mock.rs:96` (inside `pub struct MockProvider`). `new` sets `repeat: false` at `:104`; `text` (`:138`) delegates to `Self::new(vec![...])`; `texts` (`:143`) delegates to `Self::new(...)` — so all three keep `repeat` OFF transitively with UNCHANGED signatures and observable behaviour (`text`/`texts` bodies are byte-identical to baseline; only `new` gained the `repeat: false,` field initializer). This satisfies the F2.a lock.

### Claim 3 — Back-compat / ZERO external cascade: no existing call-site removed/edited; 133 baseline sites byte-identical (only NEW additive occurrences); NO existing test flips
**Verdict:** PASS
**Evidence:** `git -C … diff 929d326 -- src tests examples | grep -E '^-.*MockProvider::(new|text|texts)'` → EXIT=1 (zero matches) → no existing call-site removed or edited. Baseline count at `929d326`: `git grep -hE "MockProvider::(new|text|texts)\b" 929d326 -- src tests examples | wc -l` = **133**. Current working-tree count = **137** (+4). The +4 are all additive: the diff-visible additions of the `new|text|texts` pattern in `mock.rs` are 3 (two inline-test call-sites — `let p = MockProvider::new(vec![…])` Tier A + `let plain_new = MockProvider::new(…)` Tier F — plus the `[`MockProvider::new`]` doc cross-ref in the `with_repeat` rustdoc), and 1 in the new `tests/mock_repeat_test.rs` (Tier B `MockProvider::new(vec![tool_calls(), text("done")])`). No baseline site was mutated, so no existing test flips — consistent with the plan's predicted ZERO RED.

### Claim 4 — Kernel-minimality: forbidden-leakage grep on `mock.rs` = 0
**Verdict:** PASS
**Evidence:** `grep -rniE "i-phi|iphi|\[provider|toml|daemon|per.agent|mock_responses_as_responses|arm\.repeat" /root/projects/phi/phi-core/src/provider/mock.rs` → EXIT=1 (zero matches). The new kernel surface carries only the general test-double concepts `repeat` / "cycle" (explicitly kernel-general per plan §3, not flagged). No consumer/TOML/daemon/i-phi concept leaks into the kernel production surface.
**Observation (not a violation):** the NEW `tests/mock_repeat_test.rs` comments do reference "i-phi #121" and "i-phi's CURRENT ordering" as downstream context. This is expected and permitted — claim 4 (and the plan §3 forbidden grep) is scoped to the kernel PRODUCTION surface `src/provider/mock.rs`; explanatory context in a test file about the downstream consumer is not consumer-policy leakage into the kernel.

### Claim 5 — Semver: `Cargo.toml:3` = 0.13.0 (additive MINOR); no public signature removed/changed
**Verdict:** PASS
**Evidence:** `grep -n '^version = "0.13.0"' Cargo.toml` → `3:version = "0.13.0"`; the diff shows `-version = "0.12.0"` / `+version = "0.13.0"`. The change is a purely-additive MINOR bump: only NEW public API added (`with_repeat`, `repeating`) plus a PRIVATE field (`repeat`); no existing public signature is removed or changed (claim 2 confirms `new`/`text`/`texts`/`stream` signatures unchanged). Semver-correct.

### Claim 6 — Only `mock.rs` + `Cargo.toml` + the new test file changed in code
**Verdict:** PASS
**Evidence:** `git -C … diff --stat 929d326 -- src` → only `src/provider/mock.rs | 168 …`. `git diff --stat 929d326 -- src/agent_loop src/provider/traits.rs src/types` → empty (no changes). Working-tree status: `M Cargo.toml`, `M src/provider/mock.rs`, `?? tests/mock_repeat_test.rs` are the only CODE artifacts; the other modified/untracked paths (`docs/specs/developer/provider.md`, `docs/specs/plan/build/_cycle-index.md`, `docs/decisions/0006-…md`, the cycle folder) are DOCS (Audit B's lane). `Cargo.toml` change is the version line only. Confirms the §3 cascade-discipline claim that the only production file changed is `src/provider/mock.rs`.

### Claim 7 — §3.B LOC: logic portion within budget (~12); the +63 vs ~40 overrun is the §D6.4 rustdoc consumer-contract block + ctor doc-comments
**Verdict:** PASS (with note)
**Evidence:** `git diff --stat 929d326 -- src/provider/mock.rs` reports 165 insertions total; ~102 of those are the NEW inline `#[cfg(test)] mod tests` block (test-envelope LOC, allowed separately by §3 substrate calibration), leaving ~63 production+rustdoc. The LOGIC additions are: `repeat: bool` field (`:96`, 1 line), `new` `repeat: false` (`:104`, 1 line), `with_repeat` signature+body (`:121-126`, ~6 lines), `repeating` signature+body (`:133-135`, 3 lines), and the rotate net +4 code lines (`:209,210,214,216`) — roughly 12-15 code lines, within the §3 pause-threshold caps (F1.a `mock.rs` +≤15 field+rotate; F2.a +≤20 ctors). The remaining ~48 lines are documentation: the module-level OPT-IN + CONSUMER CADENCE CONTRACT rustdoc block (`:9-32`), the `repeat` field doc-comment (`:91-95`), the `with_repeat` doc-comment (`:108-120`), and the `repeating` doc-comment (`:128-132`). This overrun over the plan's ~40 LOC prediction is exactly the §D6.4 terminal-text-required CONSUMER contract, and is expressly permitted by plan §3.B "Doc-LOC threshold (semantic-completeness escape, v29 P-plan-7)" — the rustdoc lands all 3 content axes (repeat-mode ctor usage example + `[ToolCalls, Text]` ordering + terminal-text-required contract). Documentation density, not a logic/scope overrun.

## Workspace test result
- Command: `/root/rust-env/cargo/bin/cargo test -j 4 --manifest-path /root/projects/phi/phi-core/Cargo.toml` — **NOT-EXECUTED-IN-AUDIT** (per audit prompt: orchestrator gate-4 owns clippy/fmt/`cargo test`; phi-core host-cargo cargo-test build would populate `target/` and requires the cargo-clean discipline).
- Static cross-check (read-only): NEW test functions = 7 — 4 in `tests/mock_repeat_test.rs` (`close_gate_repeat_sustains_one_tool_call_per_send`, `one_shot_exhausts_to_fallback_on_second_send`, `toolcalls_only_repeat_is_bounded_only_by_max_turns`, `cadence_depends_on_queue_shape`) + 3 inline `#[tokio::test]` in `mock.rs` (`mock_new_is_one_shot_and_exhausts`, `mock_with_repeat_false_matches_new_one_shot`, `mock_repeat_api_equivalence`).
- Plan §8 expected delta: +7 MUST-SHIP → predicted final ≈ 617; accept band [617, 619]. Static NEW-test count (7) matches the +7 delta exactly.
- Match: ✅ (static count matches +7; live count deferred to orchestrator gate-4)

## CI guards
- N/A for phi-core — no `scripts/check-*.sh` guard set exists at v0 (per ci-guards-run project-context: phi-core has no equivalent guard set; the MUST-RUN clippy/fmt/test list IS the gate and is orchestrator-gate-4-owned). k8s-readiness-check: N/A (phi-core is a library). phi-core-leverage-check: N/A (kernel does not consume itself) — substituted by the kernel-minimality forbidden-leakage grep (claim 4, PASS).

## Final verdict
**PASS.** All 7 Audit-A claims verified with concrete file:line / grep evidence. KC-06 is a clean, purely-additive kernel test-double change: the rotate-on-pop cycle (`mock.rs:209-216`) is correct and thread-safe under the existing `Mutex`; the `repeat == false` one-shot path and the empty→fallback are behaviourally preserved; the two additive constructors + private field leave all 133 baseline call-sites byte-identical (verified: 0 removed/edited, 133→137 additive-only); zero consumer-policy leakage into the kernel surface; semver correctly bumped to 0.13.0; only `src/provider/mock.rs` changed in `src/`. Claim 7 is a PASS-with-note: the `mock.rs` +63 production+rustdoc figure exceeds the ~40 LOC prediction, but the logic portion (~12-15 LOC) is within the §3 caps and the overrun is entirely the §D6.4 consumer-contract rustdoc that plan §3.B expressly permits under the semantic-completeness escape. No failing claims. Recommendation: proceed — orchestrator closes the NOT-EXECUTED-IN-AUDIT items (fmt / clippy `-Dwarnings` / `cargo test` at [617,619]) at gate-4.
