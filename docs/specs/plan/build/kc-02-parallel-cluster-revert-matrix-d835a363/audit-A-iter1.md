<!-- Auto-generated by chunk-auditor agent. Do not edit. -->

# Audit A, iteration 1 — kc-02-parallel-cluster-revert-matrix

**Cycle hex:** d835a363
**Auditor model:** opus
**Auditor agent version:** 1
**Date:** 2026-06-09
**Plan reference:** [./plan.md](./plan.md)
**Final verdict:** PASS

## Summary

Audit A (code-correctness + tests) for KC-02 (project=phi-core kernel lane). All 8 claims verified
against the UNCOMMITTED working-tree diff vs `dev`. The F1(B) fix is a single-function change
(`retain_pinned_calls_on_node`) + a 1-token caller thread; `src/` diff stat = `src/types/context.rs`
ONLY (`+625 / −74`); `streaming.rs` + `build_trunk_context` + the node model are untouched. The
3-clause target-aware branch is implemented exactly as specified (on-trunk KEEP / off-trunk+call-node
DROP / off-trunk+result-node RE-APPEND-by-membership). The membership re-append keys on
`tool_call_id == call_id` over the node-scoped `call_ids` set (NOT the direct-child `parent_id` gate),
so a follow-on whose id ∉ this node's calls cannot match. The 3 KC-01 flips are updated in place; 10
NEW grouped/edge tests land inline (no new test file) → 30 total `collapse_abandon` tests. I executed
the full suite in-sandbox: `--lib collapse_abandon` = 30 pass / 0 fail; full lib = 244 pass / 0 fail;
full suite (28 binaries) = all ok / 0 FAILED; clippy `-Dwarnings --all-targets` clean (cache-busted via
`touch src/lib.rs`); `fmt --check` exit 0. Cleaned target/ after (5.8 GiB reclaimed). No FAIL or PARTIAL
claims.

| Claim | Verdict | Evidence-anchor |
|---|---|---|
| 1. F1(B) signature + sole caller threads `node_is_call` | PASS | context.rs:769-774 (sig), :733 (caller), :527 (node_is_call); grep = 1 def + 1 call-site |
| 2. 3-clause target-aware branch correct; membership re-append keys on `tool_call_id==call_id` | PASS | context.rs:831-852 (3 clauses), :786-802 (node-scoped call_ids), :842-848 (find on tool_call_id only) |
| 3. 4 call-node LOSS cells → `[DROP,…]`+breadcrumb | PASS | `matrix_pinned_onto_call_node_reclaims_to_breadcrumb` :3696; in-sandbox PASS |
| 4. 6 result-node LOSS cells → `[PAIR,…]` whole-cluster | PASS | `matrix_pinned_into_cluster_keeps_whole_cluster` :3737; in-sandbox PASS |
| 5. No-400 invariant all cells; backstop UNCHANGED | PASS | `matrix_no_dangling_or_orphan_across_all_cells` :4010; streaming.rs absent from src diff |
| 6. 30 total collapse_abandon (20+10 new, 3 updated, no new file) | PASS | 30 `#[test]` in module (2106→EOF); `--lib collapse_abandon` = 30 pass; src diff = context.rs only |
| 7. Kernel-minimality: 1 fn + 1 caller token; build_trunk_context + node model unchanged; self.messages never mutated | PASS | diff hunks confined to :689-852 + test module; build_trunk_context absent from diff; :842 `.iter().find()`+`.clone()` only |
| 8. clippy `-Dwarnings` clean + fmt clean | PASS | clippy finished 0 warnings (cache-busted); `fmt -- --check` exit 0 |

## Per-claim detail

### Claim 1 — F1(B) signature + caller: `retain_pinned_calls_on_node` gains `tag_on_call_node: bool`, threaded from the sole caller which passes the already-computed `node_is_call`; one signature change + one caller arg, no other call-sites.
**Verdict:** PASS
**Evidence:**
- Signature now carries the new param — `context.rs:769-774`:
  ```rust
  fn retain_pinned_calls_on_node(
      &self,
      trunk: &mut Vec<AgentMessage>,
      idx: usize,
      tag_on_call_node: bool,
  ) {
  ```
  (The plan cites ~`:769`; exact match.)
- Sole caller threads `node_is_call` — `context.rs:733`:
  `self.retain_pinned_calls_on_node(&mut trunk, cn_idx, node_is_call);` (plan cites ~`:733`; exact).
- `node_is_call` is the already-computed classifier — `context.rs:527`:
  `let node_is_call = matches!(node.message, Message::Assistant { .. });` (plan cites ~`:527`; exact).
- Exactly one definition + one call-site: `grep -rn "retain_pinned_calls_on_node" src/` →
  `context.rs:733` (call) + `context.rs:769` (def). No other call-sites anywhere in `src/`.
- The diff hunk `@@ -725,7 +730,7 @@` shows the single caller-line change
  `retain_pinned_calls_on_node(&mut trunk, cn_idx)` → `(&mut trunk, cn_idx, node_is_call)`.

### Claim 2 — The 3-clause target-aware branch is correct; the re-append find keys on `tool_call_id == call_id` (call-node membership), and a follow-on whose id ∉ this node's `call_ids` cannot match.
**Verdict:** PASS
**Evidence:**
- The per-call loop iterates `for call_id in &call_ids`, where `call_ids` is built ONLY from THIS
  node's own `Content::ToolCall` ids (`context.rs:786-802`) — node-scoped, with a deterministic
  `synth-{node}-{ci}` fallback for id-less calls. So the loop domain is exactly this call node's calls.
- Clause (i) — result already ON-trunk → `continue` (KEEP always), `context.rs:824-833`:
  ```rust
  let on_trunk = trunk.iter().any(|m| ... ToolResult { tool_call_id, .. } if tool_call_id == call_id);
  if on_trunk { continue; } // (i) call already atomic — keep
  ```
- Clause (ii) — off-trunk + `tag_on_call_node` → DROP, never re-append (reclaim), `context.rs:834-838`:
  ```rust
  if tag_on_call_node {
      ids_to_drop.push(call_id.clone());
      continue;
  }
  ```
- Clause (iii) — off-trunk + `!tag_on_call_node` → re-append by CALL-NODE MEMBERSHIP, `context.rs:842-852`:
  ```rust
  let cluster_result = self.messages.iter().find(|m| match m {
      AgentMessage::Llm(lm) => matches!(
          &lm.message,
          Message::ToolResult { tool_call_id, .. } if tool_call_id == call_id
      ),
      _ => false,
  });
  match cluster_result {
      Some(result) => results_to_append.push(result.clone()),
      None => ids_to_drop.push(call_id.clone()),
  }
  ```
  The find predicate is `tool_call_id == call_id` ONLY — there is NO `parent_id == this_node_id`
  conjunct (the old direct-child gate at the pre-diff `:802-815` was removed; `this_node_id` is now
  retained only via `let _ = this_node_id;` at `:820`). Because the outer loop's `call_id` ranges over
  this node's `call_ids` set exclusively, a genuine post-cluster follow-on (whose `tool_call_id` ∉
  `call_ids`) is never iterated, hence never re-appended → stays dropped/sealed-past. The diff hunk
  `@@ -797,19 +829,24 @@` confirms the rename `direct_child` → `cluster_result` and the predicate
  reduction. The behavioural distinction is also exercised by the `edge_post_cluster_followon_dropped`
  test (`:3909`), which asserts the sealed-past sequential `seq_c` is dropped while cluster siblings
  re-append — in-sandbox PASS.

### Claim 3 — The 4 call-node LOSS cells flip to `[DROP,…]`+breadcrumb (`matrix_pinned_onto_call_node_reclaims_to_breadcrumb`, arity-2 + arity-3, Outcome + Checkpoint).
**Verdict:** PASS
**Evidence:**
- Test `matrix_pinned_onto_call_node_reclaims_to_breadcrumb` at `context.rs:3696`. Loops
  `for kind in [TagKind::Outcome, TagKind::Checkpoint]` × {arity-2 via `parallel_fixture(NodeId(1),1,kind)`,
  arity-3 via `parallel_fixture_3(NodeId(1),1,kind)`} = 4 call-node cells. Per cell asserts:
  - both/all off-trunk siblings reclaimed: `!trunk_has_tool_result("pcall_a") && !..("pcall_b")` (+ `pcall_c` for a3);
  - no surviving tool-call: `!trunk_has_any_tool_call(&r2)` (whole cluster reclaimed);
  - breadcrumb present: `trunk_has_breadcrumb(&r, "reverted past: parallel cluster")`;
  - no-400: `assert_no_400(...)` (both `!trunk_has_dangling_call` + `!trunk_has_orphan_result`).
- In-sandbox: `cargo test --lib collapse_abandon` → this test `... ok`. Renders through the verbatim
  production pipeline incl. backstop (`render_with_backstop` → `render_pipeline` + `enforce_call_atomic_backstop`).

### Claim 4 — The 6 result-node LOSS cells flip to `[PAIR,…]` whole-cluster (`matrix_pinned_into_cluster_keeps_whole_cluster`, first-result a2+a3, mid-result a3).
**Verdict:** PASS
**Evidence:**
- Test `matrix_pinned_into_cluster_keeps_whole_cluster` at `context.rs:3737`. Loops Outcome+Checkpoint ×
  {first-result a2 `parallel_fixture(NodeId(2),2)`, first-result a3 `parallel_fixture_3(NodeId(2),2)`,
  mid-result a3 `parallel_fixture_3(NodeId(3),3)`} = 6 cells. Per cell asserts every off-trunk sibling
  re-appended (`trunk_has_tool_result` true for pcall_a/b[/c]) + `assert_no_400`. The mid-result a3 case
  explicitly checks the sibling PAST the revert tip (`pcall_c`) re-appends by membership.
- In-sandbox: this test `... ok`.

### Claim 5 — No-400 invariant across all cells (`matrix_no_dangling_or_orphan_across_all_cells`); `enforce_call_atomic_backstop` (`streaming.rs:130`) is UNCHANGED (absent from diff).
**Verdict:** PASS
**Evidence:**
- Test `matrix_no_dangling_or_orphan_across_all_cells` at `context.rs:4010`. Sweeps all 4 kinds
  (Lesson, Finding, Outcome, Checkpoint) × arity-2 targets {1,2,3} + arity-3 targets {1,2,3,4},
  calling `assert_no_400` per cell — which asserts `!trunk_has_dangling_call(trunk)` AND
  `!trunk_has_orphan_result(trunk)` (the new orphan detector at `:3573`). In-sandbox `... ok`.
- `enforce_call_atomic_backstop` confirmed present at `streaming.rs:130`
  (`pub(crate) fn enforce_call_atomic_backstop(...)`).
- `git diff --stat dev -- src/` reports `src/types/context.rs` as the ONLY changed file
  (`1 file changed, 625 insertions(+), 74 deletions(-)`). `streaming.rs` does not appear → backstop
  byte-identical / UNCHANGED. Confirmed.

### Claim 6 — Existing test posture: 20 existing → 17 unchanged-green + 3 updated + 10 NEW = 30 total `collapse_abandon`; NO test file added (inline in `context.rs`).
**Verdict:** PASS
**Evidence:**
- Count of `#[test]` fns inside `mod collapse_abandon_class_cluster_tests` (starts `:2106`, runs to EOF):
  `awk 'NR>=2106 && /^    #\[test\]/' | wc -l` → 30.
- The 10 NEW test fns are all present (verified by grep): `matrix_pinned_onto_call_node_reclaims_to_breadcrumb`,
  `matrix_pinned_into_cluster_keeps_whole_cluster`, `matrix_on_trunk_pinned_cluster_kept_always`,
  `matrix_abandon_class_call_node_first_result_symmetric`, `matrix_abandon_class_asymmetry_is_benign`,
  `matrix_pinned_last_result_and_post_cluster_no_loss`, `edge_post_cluster_followon_dropped`,
  `edge_mixed_on_off_trunk`, `edge_two_cluster_isolation`, `matrix_no_dangling_or_orphan_across_all_cells`.
  20 baseline + 10 NEW = 30. (Plan §3.C cites 8-12 NEW band; actual 10 → within band.)
- The 3 KC-01 flips are UPDATED in place (not added/removed; net count change 0):
  `pinned_class_keeps_cluster_whole_no_dangling_call` (:2495 region, assertion INVERTED to `!has_result`
  + breadcrumb), `row_pinned_revert_to_first_result_now_call_atomic` (now asserts `pcall_b` re-appended →
  `[PAIR,PAIR]`), `row_pinned_revert_to_call_node_now_call_atomic` (now asserts both reclaimed + breadcrumb).
  Diff hunks `@@ -2473,16 +2516,7 @@`, `@@ -3245,18 +3295,23 @@`, `@@ -3268,14 +3323,19 @@` confirm
  these are in-place body edits, not new fns.
- NO new test file: `git status` shows only `src/types/context.rs` modified in src + the untracked ADR
  `.md`; the `git diff --stat` confirms a single src file. Tests are inline in `context.rs`.
- In-sandbox: `cargo test --lib collapse_abandon` → `30 passed; 0 failed; 0 ignored`.

### Claim 7 — Kernel-minimality (§3.A): blast radius = one fn + 1 caller token; `build_trunk_context` (~:1074) + node model UNCHANGED; forensic `self.messages` NEVER mutated; no public-API / persisted-field / migration change; zero consumer leakage.
**Verdict:** PASS
**Evidence:**
- Diff hunk headers confine all production changes to `:689-740` (the pinned-branch comment + the
  caller-thread at :733) and `:784-852` (the helper's call_ids + the 3-clause branch). Everything from
  `:2454+` is inside `mod collapse_abandon_class_cluster_tests`. No hunk touches the
  `build_trunk_context` region (now at `:1111`; plan cites ~:1074) — `build_trunk_context` does NOT
  appear in the production-side diff. Node model (`NodeId`, `with_node_identity`, `parent_id`) unchanged.
- `self.messages` read-only inside `retain_pinned_calls_on_node`: the only access is `:842`
  `self.messages.iter().find(...)` followed by `.clone()` at `:850`. No `&mut self.messages`, no
  `.push/.retain/.insert/.remove` on `self.messages` anywhere in the fn (the only mutation is on the
  local `trunk` via `content.retain` :858 and `trunk.insert` :867 — the cloned render output, not the
  forensic log). The fn takes `&self` (`:770`), so mutation of `self.messages` is impossible by type.
- No public-API change: `retain_pinned_calls_on_node` is a private `fn` (no `pub`); the signature change
  is crate-internal. No persisted-field / migration change (phi-core is a library; no schema). Crate
  version stays 0.11.4.
- Zero consumer leakage: the change is render-correctness in the brake collapse path; no i-phi/baby-phi
  requirement leaks in. Per plan §3.A verdict (cite P0 §4 / F-6 / F-8 / §10.6).
- Kernel-minimality surface-discipline (substitute for phi-core-leverage-check, which is N/A — kernel
  does not consume itself): SATISFIED. Blast radius = single private fn + 1 caller arg token.

### Claim 8 — clippy `-Dwarnings --all-targets` clean + `fmt -- --check` clean.
**Verdict:** PASS
**Evidence:**
- `RUSTFLAGS="-Dwarnings" cargo clippy --all-targets -j 4` → after `touch src/lib.rs` to bust the
  incremental cache, re-checked the full tree: `Checking phi-core v0.11.4 ... Finished dev profile ...
  in 4.53s` — zero warnings/errors (any warning would fail under `-Dwarnings`).
- `cargo fmt -- --check` → no output, `FMT_EXIT=0` (clean).
- Note: these were EXECUTED in the audit sandbox (not blocked). The orchestrator's gate-4 MUST-RUN list
  remains authoritative; this in-shell success is a positive cross-check.

## Workspace test result
- Command: `/root/rust-env/cargo/bin/cargo test -j 4 --lib collapse_abandon` (+ full `--lib` + full suite)
- Output: `--lib collapse_abandon` = **30 passed / 0 failed / 0 ignored** (214 filtered out);
  full `--lib` = **244 passed / 0 failed**; full suite (28 test binaries) = all `test result: ok`,
  **0 FAILED** (session_test 36, agent_loop/agent/tools etc. all green; 2 integration tests ignored
  as expected — live-API).
- Plan §8 expected delta: `collapse_abandon` +8 to +12 (20 → 28-32); 3 UPDATED do not change count.
- Match: ✅ (actual +10 → 30, within `[8,12]` band; 3 flips updated in place)
- target/ cleaned after run: `cargo clean` removed 6285 files / 5.8 GiB.

## CI guards
- phi-core has NO `scripts/check-*.sh` CI guard set at v0 (ships only a `scripts/pre-commit` fmt+clippy
  hook). The MUST-RUN list (clippy + test + fmt) IS the gate and was executed (claim 8 + workspace test).
- check-doc-links.sh: N/A (not present for phi-core)
- check-ops-doc-headers.sh: N/A
- check-phi-core-reuse.sh: N/A (kernel does not consume itself; phi-core-leverage-check is N/A)
- check-spec-drift.sh: N/A
- k8s-readiness-check: N/A (phi-core is a library, not a daemon — all 7 axes N/A)

## Final verdict
**PASS.** All 8 Audit-A claims verified with file:line evidence against the uncommitted working-tree
diff. The F1(B) fix is implemented exactly as the plan + P0 §10 specify: a single private function
(`retain_pinned_calls_on_node`) gains one `tag_on_call_node: bool` param threaded from the sole caller's
already-computed `node_is_call`; the 3-clause target-aware branch (on-trunk KEEP / off-trunk+call-node
DROP-reclaim / off-trunk+result-node RE-APPEND by call-node membership) is correct, with the re-append
keying on `tool_call_id == call_id` over the node-scoped `call_ids` set (the direct-child `parent_id`
gate removed), so follow-ons outside the node's calls cannot match. The 4 call-node LOSS cells flip to
`[DROP,…]`+breadcrumb, the 6 result-node LOSS cells to `[PAIR,…]` whole-cluster, and the no-400
invariant holds across all swept cells with the `streaming.rs` backstop UNCHANGED. Test posture: 30
total `collapse_abandon` tests (20 baseline + 10 NEW, 3 updated in place, no new test file), all green
in-sandbox; full lib 244/0 and full suite all-ok. Kernel-minimality holds (1 fn + 1 caller token;
`build_trunk_context` + node model untouched; forensic `self.messages` read-only via `&self`); clippy
`-Dwarnings` + fmt clean. No FAIL or PARTIAL claims.
