<!-- Auto-generated by chunk-auditor agent. Do not edit. -->

# Audit C, iteration 1 — kc-02-parallel-cluster-revert-matrix

**Cycle hex:** d835a363
**Auditor model:** opus
**Auditor agent version:** 1
**Date:** 2026-06-09
**Plan reference:** [./plan.md](./plan.md)
**Final verdict:** PASS

## Summary

Audit C verifies the carry-forward regression posture of the F1(B) fix: the 3 KC-01 test-flips are the INTENDED semantic change (not collateral), the load-bearing on-trunk-keep gate holds, the SAFE rows + KC-01-era atomicity tests stay green unchanged, the no-400 invariant holds across all matrix + edge cells, the full suite is green, and the plan's live-grid close-gate names both dispositions.

The diff vs `dev` touches ONLY `src/types/context.rs` (+625 / −74). `src/agent_loop/streaming.rs` is UNTOUCHED (claim 4 backstop invariant). The production change is a single function (`retain_pinned_calls_on_node`) plus a 1-token caller thread. In the test module, exactly 3 existing KC-01 test bodies were modified (the intended flips) and 10 NEW matrix/edge tests added; the gate test, the 3 SAFE rows, and `collapse_keeps_messages_byte_identical` are absent from every modified hunk → unchanged.

All 6 claims PASS. The cargo executions were NOT sandbox-blocked in this audit shell; results are recorded as PASS-with-caveat (orchestrator gate-4 MUST-RUN list remains authoritative). collapse_abandon: 30 passed / 0 failed. Full suite: lib 244 / 0, agent_loop_test 56 / 0, agent_test 14 / 0, session_test 36 / 0; 0 FAILED across 23 non-empty test binaries — matching the orchestrator's gate-2 numbers exactly.

| Claim | Verdict | Evidence-anchor |
|---|---|---|
| 1. Exactly 3 KC-01 tests flipped + intended (not collateral) | PASS | context.rs:2501 (`!has_result` call_abc + breadcrumb), :3316 (`[DROP,DROP]`+crumb), :3286 (`[PAIR,PAIR]`); diff hunks confirm only these 3 + production fn modified |
| 2. On-trunk-keep gate load-bearing (gate test unchanged + NEW probe) | PASS | context.rs:3061 gate test ABSENT from diff hunks; clause (i) at fn :831-833; NEW `matrix_on_trunk_pinned_cluster_kept_always` :3778 |
| 3. 3 SAFE rows + KC-01-era atomicity tests green unchanged | PASS | :3345/:3364/:3382 + :2546 absent from diff hunks; full lib 244 pass |
| 4. No-400 across all cells (matrix + 5 edges); backstop unchanged | PASS | `matrix_no_dangling_or_orphan_across_all_cells` :4010 + 3 edge tests :3909/:3949/:3966; streaming.rs untouched (empty diff) |
| 5. Full suite green | PASS-with-caveat | lib 244 / agent_loop 56 / agent_test 14 / session_test 36, 0 fail (cargo ran in audit shell) |
| 6. Live-grid close-gate posture states both dispositions | PASS | plan.md §10 "Post-fix close-gate" lines 240-241 (call-node reclaims + result-node keeps-whole, both drivers × 4 cats, transcript-read, no-400) |

## Per-claim detail

### Claim 1 — Exactly 3 KC-01 tests flipped, and they are the INTENDED change (not collateral)
**Verdict:** PASS
**Evidence:**

`pinned_class_keeps_cluster_whole_no_dangling_call` (`context.rs:2501`) — the M=1 call-node case. The body now asserts the INVERTED (B) disposition:
- `:2520-2532` — `let has_result = collapsed.iter().any(... tool_call_id == "call_abc")` then `assert!(!has_result, "(B) off-trunk + tag-on-call-node reclaims: the result is NOT re-appended")`.
- `:2534-2537` — `assert!(!trunk_has_dangling_call(&collapsed), ...)`.
- `:2538-2542` — `assert!(trunk_has_breadcrumb(&collapsed, "reverted past: sub-task sealed"), ...)`.

The diff hunk `@@ -2473,16 +2516,7` confirms the prior "keep the sealed body whole" assertion was REMOVED: removed lines include `trunk_has_heavy_args(&collapsed, "PLAN-V1-HEAVY-BODY")`. This is the most consequential flip (M=1 keep-whole INVERTED to `[DROP]`+crumb) and matches the audit-prompt expectation. Not a weakened/trivially-true assertion — it asserts specific absence of `call_abc`'s result + specific breadcrumb text.

`row_pinned_revert_to_call_node_now_call_atomic` (`context.rs:3316`) — arity-2 call-node. Body at `:3322-3339`:
- `!trunk_has_dangling_call` + `!trunk_has_tool_result(&collapsed, "pcall_a")` + `!trunk_has_tool_result(&collapsed, "pcall_b")` + `trunk_has_breadcrumb(&collapsed, "reverted past: parallel cluster")`. This is `[DROP,DROP]`+crumb (was `[PAIR,DROP]`). Diff hunk `@@ -3268,14 +3323,19` removes `trunk_has_tool_result(&collapsed, "pcall_a")` (the old keep). Specific, not weakened.

`row_pinned_revert_to_first_result_now_call_atomic` (`context.rs:3286`) — arity-2 first-result. Body at `:3294-3307`:
- `!trunk_has_dangling_call` + `trunk_has_tool_result(&collapsed, "pcall_a")` (on-trunk kept) + `trunk_has_tool_result(&collapsed, "pcall_b")` (re-appended by membership). This is `[PAIR,PAIR]` (was `[PAIR,DROP]`). Diff hunk `@@ -3245,18 +3295,23` removes the old `has_call_a_result` assertion. Specific, not weakened — exactly the user-confirmed "→ now `[PAIR,PAIR]`" prediction (plan §6 row 3).

**Only-3 verification:** `git diff dev -- src/types/context.rs | grep '^-.*fn ...(' ` returns ONLY `retain_pinned_calls_on_node` (the production fn signature change). No test-fn signature was removed. The diff hunk ranges are: production fn (old 689-829), the three flipped tests (old 2454-2509 + 3231-3282), and the appended new tests (old 3483+). No other existing test fn falls inside a modified hunk. The audit-prompt note "+ are the ONLY flips" holds: no collateral test bodies changed.

### Claim 2 — On-trunk-keep gate (load-bearing)
**Verdict:** PASS
**Evidence:**

`pinned_cluster_mid_trunk_kept_verbatim` (`context.rs:3061`) body (`:3061-3103`) is UNCHANGED: it does not appear as an added (`+`) or removed (`-`) `fn` line in the diff, and it sits between diff hunks `@@ -2493,8` and `@@ -3231,12` (old line ~3000) → not inside any modified hunk. Its body still drives `render_pipeline` across turns [1, 100] asserting `trunk_has_heavy_args("SEALED-RESULT-BODY")` kept verbatim.

The gate clause is implemented at `retain_pinned_calls_on_node:831-833`: `let on_trunk = trunk.iter().any(... ToolResult tool_call_id == call_id); if on_trunk { continue; // (i) call already atomic — keep }` — clause (i), the load-bearing mid-trunk gate. The doc-comment at `:807-809` labels it "Load-bearing on-trunk gate."

NEW explicit probe `matrix_on_trunk_pinned_cluster_kept_always` (`context.rs:3778`) guards clause (i) directly: for both pinned kinds it drives last-result a2 (`parallel_fixture(NodeId(3),3,kind)`) and a3 (`parallel_fixture_3(NodeId(4),4,kind)`) through `render_with_backstop` and asserts all on-trunk siblings kept (`trunk_has_tool_result` for pcall_a/b/c) + `trunk_has_any_tool_call` (calls not reclaimed) + `assert_no_400`. Runtime: passes (collapse_abandon green).

### Claim 3 — 3 SAFE KC-01 rows + KC-01-era atomicity tests stay green unchanged
**Verdict:** PASS
**Evidence:**

The 3 SAFE rows — `row_abandon_revert_to_first_result_stays_safe` (`:3345`), `row_abandon_revert_to_call_node_stays_safe` (`:3364`), `row_revert_to_last_result_stays_safe` (`:3382`) — sit between diff hunks `@@ -3268,14` and `@@ -3483,4` (old lines ~3282-3470) → NOT inside any modified hunk → bodies unchanged. None appears as a `+`/`-` `fn` line in the diff.

`collapse_keeps_messages_byte_identical` (`:2546`) and `collapse_keeps_messages_byte_identical_after_multi_turn_scan` (`:3110`) likewise absent from modified hunks (the immutable-log carry-forward invariants).

The KC-01-era abandon/atomicity tests (`abandon_class_collapses_heavy_cluster_to_breadcrumb:2319`, `abandon_class_collapses_when_tip_is_tool_result_collapses_whole_cluster:2410`, `continue_after_revert_pointer_is_never_dangling:2076`, etc.) are all outside modified hunks. Runtime confirms: `cargo test --lib collapse_abandon` → 30 passed / 0 failed (includes the module's 28 + 2 matched from a sibling module: `r4_empty_node_after_per_call_retain_is_dropped`, `r3_mixed_class_pin_wins_keeps_node_whole`).

### Claim 4 — No-400 across all cells (matrix + 5 edges); backstop unchanged
**Verdict:** PASS
**Evidence:**

`git diff dev --stat -- src/agent_loop/streaming.rs` returns EMPTY → `enforce_call_atomic_backstop` (streaming.rs:130) UNCHANGED.

`trunk_has_orphan_result` detector authored at `:3639` (mirror of `trunk_has_dangling_call`). `assert_no_400` helper at `:3681` asserts BOTH `!trunk_has_dangling_call` AND `!trunk_has_orphan_result`. The matrix/edge tests run through `render_with_backstop` (`:3670`) which applies `enforce_call_atomic_backstop` after `render_pipeline` — the verbatim production order.

`matrix_no_dangling_or_orphan_across_all_cells` (`:4010`) sweeps 4 categories (Lesson/Finding/Outcome/Checkpoint) × (3 arity-2 targets + 4 arity-3 targets) = 28 cells, each `assert_no_400`. The 3 edge tests assert `assert_no_400`: `edge_post_cluster_followon_dropped` (`:3909` — membership distinguishes cluster-sibling vs sealed-past follow-on), `edge_mixed_on_off_trunk` (`:3949` — off-trunk sibling past tip re-appended), `edge_two_cluster_isolation` (`:3966` — upstream kept verbatim, tip cluster whole). The M=1 + KC01-flip probes are covered by the Tier-A flip tests (plan §8 — so the "5 edges" = 3 named edge tests + M=1 + KC01-flip via the flip tests). Runtime: all green.

### Claim 5 — Full suite green
**Verdict:** PASS-with-caveat (observed PASS in audit shell; orchestrator gate-4 MUST-RUN remains authoritative)
**Evidence:**

`/root/rust-env/cargo/bin/cargo test -j 4 --manifest-path /root/projects/phi/phi-core/Cargo.toml`:
- `unittests src/lib.rs`: **244 passed; 0 failed; 0 ignored**.
- `tests/agent_loop_test.rs`: **56 passed; 0 failed**.
- `tests/agent_test.rs`: **14 passed; 0 failed**.
- `tests/session_test.rs`: **36 passed; 0 failed**.
- All other binaries green; `integration_anthropic.rs` 0 passed / 3 ignored (live-key, expected). 0 FAILED across 23 non-empty test binaries.

These match the orchestrator's gate-2/gate-4 numbers verbatim (lib 244, collapse_abandon 30, agent_loop 56, agent_test 14, session_test 36). cargo clean run post-test per discipline. clippy + fmt were NOT independently run in this audit (orchestrator MUST-RUN per CLAUDE.md gate-4; reported clean at gate-2).

### Claim 6 — Live-grid close-gate posture stated
**Verdict:** PASS
**Evidence:**

Plan `§10` "Post-fix close-gate (orchestrator-run, the real close...)" (lines 240-241) states: "the FULL live grid — both drivers (`deepseek-chat-v3-0324` + `minimax-m2.7`) × the 4 categories — transcript-read, asserting correct disposition end-to-end: **call-node reverts reclaim to the breadcrumb** (sealed-past cluster summarised, no half-keep) AND **result-node reverts KEEP the whole cluster** (no sibling lost, no re-fetch loop); no 400 in any cell." Both dispositions named, both drivers × 4 categories, transcript-read, no-400 all specified. The auditor confirms the criteria are STATED (orchestrator executes post-seal). The §10 "Pre-fix live confirmation" (line 239) additionally establishes the live S2 consequence. PASS.

## Workspace test result
- Command: `/root/rust-env/cargo/bin/cargo test -j 4 --manifest-path /root/projects/phi/phi-core/Cargo.toml`
- Output: lib 244 passed / 0 failed / 0 ignored; agent_loop_test 56 / 0; agent_test 14 / 0; session_test 36 / 0; collapse_abandon (targeted) 30 / 0; 0 FAILED total.
- Plan §8 expected delta: `collapse_abandon` 20 → 28-32 (+8 to +12 NEW grouped/edge; 3 UPDATED net 0). Actual: 10 NEW matrix/edge tests; targeted run 30 (28 module + 2 sibling-module matches) — within band.
- Match: ✅

## CI guards
- phi-core has NO `scripts/check-*.sh` CI guard set at v0 (plan §0 / CLAUDE.md phi-core MUST-RUN note). N/A — not applicable to this project.
- check-doc-links.sh: N/A (phi-core)
- check-ops-doc-headers.sh: N/A (phi-core)
- check-phi-core-reuse.sh: N/A (kernel does not consume itself)
- check-spec-drift.sh: N/A (phi-core)

## Final verdict
**PASS.** All 6 Audit-C claims pass. The carry-forward regression posture is intact: the diff touches only `context.rs`; exactly 3 KC-01 test bodies were flipped to the INTENDED (B) dispositions (`pinned_class_keeps_cluster_whole_no_dangling_call` → `[DROP]`+crumb with the M=1 keep-whole assertion INVERTED; `row_pinned_revert_to_call_node_now_call_atomic` → `[DROP,DROP]`+crumb; `row_pinned_revert_to_first_result_now_call_atomic` → `[PAIR,PAIR]` by membership re-append) — none collateral, none weakened. The load-bearing on-trunk-keep gate (clause i at fn :831-833) holds: the gate test `pinned_cluster_mid_trunk_kept_verbatim` is unchanged and a NEW explicit probe guards it. The 3 SAFE rows + the KC-01-era atomicity/byte-identity tests are unchanged and green. The no-400 invariant is asserted across all matrix + edge cells via `render_with_backstop` + `assert_no_400`, with `enforce_call_atomic_backstop`/streaming.rs untouched. Full suite green (lib 244 + integration 56/14/36, 0 fail). The plan §10 live-grid close-gate names both dispositions. No regression-candidate findings; no flips look like collateral. Claim 5 is PASS-with-caveat solely because the orchestrator's gate-4 cargo/clippy run is the canonical signal — the in-shell run was a positive cross-check that succeeded.
